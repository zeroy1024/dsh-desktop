diff --git a/packages/session-query/session-query-sqlite/src/index.ts b/packages/session-query/session-query-sqlite/src/index.ts
index 4722557..66fccdd 100644
--- a/packages/session-query/session-query-sqlite/src/index.ts
+++ b/packages/session-query/session-query-sqlite/src/index.ts
@@ -10,7 +10,6 @@ import { Context, Service, type Fiber } from '@deepseek-ai/cordis'
 import z from '@deepseek-ai/schemastery'
 import { SessionSeq } from '@deepseek-ai/dsh-session'
 import type {
-  Session,
   SessionEvent,
   SessionHeader,
   SessionId,
@@ -27,7 +26,6 @@ import SessionQueryEngine, {
   SessionQueryError,
   SessionSearchCursor,
   assertSessionHeadersCompatible,
-  buildSessionEventSearchDocuments,
 } from '@deepseek-ai/dsh-session-query'
 import type {
   Config as SessionQueryConfig,
@@ -320,6 +318,18 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
     })
   }
 
+  /**
+   * Stable identity of this provider's semantic-document projection.
+   * Override when buildSearchDocuments changes; keep constant during an instance's
+   * lifetime. A different identity rebuilds persisted documents even if raw logs
+   * have not changed. Exact reads and the canonical session store are unaffected.
+   */
+  protected get searchDocumentVersion(): string { return '1' }
+
+  private _indexedRevision(revision: SessionPersistenceRevision): string {
+    return JSON.stringify([this.searchDocumentVersion, revision])
+  }
+
   /** Close the database after every accepted operation reaches quiescence. */
   close(): Promise<void> {
     this._closePromise ??= this._close()
@@ -506,7 +516,7 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
           assertNotAborted(signal)
           persisted = materializePersistenceSnapshots(before)
           for (const entry of persisted.values()) {
-            if (canReuseIndexed && indexed.get(entry.header.id)?.revision === entry.revision) continue
+            if (canReuseIndexed && indexed.get(entry.header.id)?.revision === this._indexedRevision(entry.revision)) continue
             // Skip work already shadowed by a live owner. `inspect()` is
             // non-mutating, so an owner attaching after this check cannot cause
             // crash-repair side effects; the live-membership retry below makes
@@ -520,6 +530,7 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
               loaded.meta,
               loaded.inheritedEventCount,
               loaded.events,
+              (id, events) => this.buildSearchDocuments(id, events),
             )
           }
           assertNotAborted(signal)
@@ -545,7 +556,12 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
       }
       const live = new Map<SessionId, ObservedSession>()
       for (const session of this.ctx.sessions.list()) {
-        const observed = observeLive(session)
+        const observed = observeSession(
+          session.header,
+          session.inheritedEventCount,
+          session.snapshotEvents(),
+          (id, events) => this.buildSearchDocuments(id, events),
+        )
         const durable = persisted.get(session.id)
         if (durable !== undefined) assertSessionHeadersCompatible(observed.header, durable.header)
         live.set(session.id, observed)
@@ -590,7 +606,7 @@ export class SqliteSessionQueryEngine extends SessionQueryEngine {
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
     `).run(
       ...headerBindings(entry.header, entry.inheritedEventCount),
-      revision,
+      this._indexedRevision(revision),
       generation,
     )
     const insert = db.prepare(`
@@ -868,21 +884,18 @@ function selectedDocumentsParams(query: string, persistenceVisible: boolean): Ar
   ]
 }
 
-function observeLive(session: Session): ObservedSession {
-  return observeSession(session.header, session.inheritedEventCount, session.snapshotEvents())
-}
-
 function observeSession(
   header: SessionHeader,
   inheritedEventCount: SessionLogOffset,
   events: readonly SessionEvent[],
+  buildDocuments: (id: SessionId, events: readonly SessionEvent[]) => SessionEventSearchDocument[],
 ): ObservedSession {
   const detachedHeader = structuredClone(header)
   const detachedEvents = events.map(event => structuredClone(event))
   return {
     header: detachedHeader,
     inheritedEventCount,
-    documents: buildSessionEventSearchDocuments(detachedHeader.id, detachedEvents),
+    documents: buildDocuments(detachedHeader.id, detachedEvents),
     fingerprint: createHash('sha256')
       .update(JSON.stringify({ header: detachedHeader, inheritedEventCount, events: detachedEvents }))
       .digest('base64url'),
diff --git a/packages/session-query/session-query-sqlite/tests/document-projection.spec.ts b/packages/session-query/session-query-sqlite/tests/document-projection.spec.ts
new file mode 100644
index 0000000..0e2742a
--- /dev/null
+++ b/packages/session-query/session-query-sqlite/tests/document-projection.spec.ts
@@ -0,0 +1,65 @@
+import { afterEach, describe, expect, it } from 'vitest'
+import { Context, type Fiber } from '@deepseek-ai/cordis'
+import { createUserMessage } from '@deepseek-ai/dsh-llm'
+import SessionStore, { Session, SessionId, SessionSeq, type SessionEvent } from '@deepseek-ai/dsh-session'
+import SessionProjectionRegistry from '@deepseek-ai/dsh-session-projection'
+import JsonlSessionPersistence from '@deepseek-ai/dsh-session-persistence-jsonl'
+import SqliteSessionQueryEngine from '@deepseek-ai/dsh-session-query-sqlite'
+import { mkdtemp, rm } from 'node:fs/promises'
+import { tmpdir } from 'node:os'
+import { join } from 'node:path'
+
+class FirstDocumentQuery extends SqliteSessionQueryEngine {
+  protected override get searchDocumentVersion(): string { return 'first-document-v1' }
+  protected override buildSearchDocuments(id: SessionId, events: readonly SessionEvent[]) {
+    return super.buildSearchDocuments(id, events).filter(document => document.seq === 0)
+  }
+}
+
+const fibers: Fiber[] = []
+const paths: string[] = []
+afterEach(async () => {
+  for (const fiber of fibers.splice(0).toReversed()) await fiber.dispose()
+  for (const path of paths.splice(0)) await rm(path, { recursive: true, force: true })
+})
+
+async function fixture() {
+  const root = await mkdtemp(join(tmpdir(), 'dsh-document-projection-'))
+  paths.push(root)
+  const ctx = new Context()
+  fibers.push(await ctx.plugin(SessionProjectionRegistry))
+  fibers.push(await ctx.plugin(SessionStore))
+  fibers.push(await ctx.plugin(JsonlSessionPersistence, { root: join(root, 'sessions'), compression: 'none' }))
+  const session = Session.create(SessionId('documents'))
+  for (const text of ['retained needle', 'excluded needle']) {
+    session.append('user/message', createUserMessage({ content: [{ type: 'text', text }], source: { kind: 'user' } }), { surfaceOp: 'append' })
+  }
+  await ctx.sessionPersistence.create(session.header)
+  await ctx.sessionPersistence.append(session.id, session.snapshotEvents())
+  return { ctx, session, config: { path: join(root, 'query.db') } }
+}
+
+describe('semantic-document provider extension', () => {
+  it('shares filtering with full-text indexing, without changing exact history reads', async () => {
+    const { ctx, session, config } = await fixture()
+    fibers.push(await ctx.plugin(FirstDocumentQuery, config))
+    expect((await ctx.sessionQuery.searchEvents({ sessionId: session.id, query: 'needle' })).items.map(hit => hit.seq)).toEqual([0])
+    expect((await ctx.sessionQuery.searchSessions({ query: 'excluded' })).items).toEqual([])
+    expect((await ctx.sessionQuery.filterEvents(session.id, [])).map(document => document.seq)).toEqual([0])
+    expect((await ctx.sessionQuery.readEvent({ sessionId: session.id, seq: SessionSeq(1) })).target.data).toMatchObject({ content: [{ text: 'excluded needle' }] })
+    const live = ctx.sessions.create(SessionId('live-documents'), { seed: session.snapshotEvents() })
+    expect((await ctx.sessionQuery.searchEvents({ sessionId: live.id, query: 'excluded' })).items).toEqual([])
+  })
+
+  it('rebuilds unchanged persisted logs when switching projection versions in either direction', async () => {
+    const { ctx, session, config } = await fixture()
+    const native = await ctx.plugin(SqliteSessionQueryEngine, config)
+    expect((await ctx.sessionQuery.searchEvents({ sessionId: session.id, query: 'needle' })).items).toHaveLength(2)
+    await native.dispose()
+    const projected = await ctx.plugin(FirstDocumentQuery, config)
+    expect((await ctx.sessionQuery.searchEvents({ sessionId: session.id, query: 'needle' })).items.map(hit => hit.seq)).toEqual([0])
+    await projected.dispose()
+    fibers.push(await ctx.plugin(SqliteSessionQueryEngine, config))
+    expect((await ctx.sessionQuery.searchEvents({ sessionId: session.id, query: 'needle' })).items).toHaveLength(2)
+  })
+})
diff --git a/packages/session-query/session-query/src/index.ts b/packages/session-query/session-query/src/index.ts
index 8d533f6..41358e8 100644
--- a/packages/session-query/session-query/src/index.ts
+++ b/packages/session-query/session-query/src/index.ts
@@ -9,6 +9,7 @@ import {
   Session,
   SessionSeq,
   snapshotSessionEvent,
+  type SessionEvent,
   type SessionId,
   type SessionSeq as SessionSeqType,
 } from '@deepseek-ai/dsh-session'
@@ -247,6 +248,20 @@ export abstract class SessionQueryEngine extends Service {
     }, signal)
   }
 
+  /**
+   * Project semantic documents for filtering and full-text indexing.
+   * Providers may override this without changing exact raw-log reads or traces.
+   * @param sessionId - session owning the complete contiguous event log.
+   * @param events - complete raw log, retaining its original sequence numbers.
+   * @returns searchable documents in ascending sequence order.
+   */
+  protected buildSearchDocuments(
+    sessionId: SessionId,
+    events: readonly SessionEvent[],
+  ): SessionEventSearchDocument[] {
+    return buildSessionEventSearchDocuments(sessionId, events)
+  }
+
   /**
    * List lightweight raw-log event records for one logical session.
    * @param sessionId - live-preferred session id to read.
@@ -283,7 +298,7 @@ export abstract class SessionQueryEngine extends Service {
     filters: readonly SessionEventResultFilter[],
   ): Promise<SessionEventSearchDocument[]> {
     const loaded = await this._corpus.load(sessionId)
-    const documents = buildSessionEventSearchDocuments(sessionId, loaded.events)
+    const documents = this.buildSearchDocuments(sessionId, loaded.events)
     return filterSessionEventDocuments(documents, filters)
   }
 
