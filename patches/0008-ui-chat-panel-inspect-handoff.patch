diff --git a/packages/client/ui-chat/src/client/apply.ts b/packages/client/ui-chat/src/client/apply.ts
index 0c7d87f..77739e8 100644
--- a/packages/client/ui-chat/src/client/apply.ts
+++ b/packages/client/ui-chat/src/client/apply.ts
@@ -139,6 +139,17 @@ export function apply(ctx: Context): void {
             },
             read: () => chatScrollPositions.get(sessionId) ?? null,
           },
+          // patches/0008: desktop panel handoff for Inspect — the panel
+          // service resolves per call (lazy like fileMentions), so composing
+          // the panel shell in or out takes effect live.
+          inspectHandoff: (callId) => {
+            const handoff = (ctx.get('panelShell') as
+              | { inspect?: (pageId: string, callId: string) => void }
+              | undefined)?.inspect
+            if (handoff === undefined) return false
+            handoff('trajectory', callId)
+            return true
+          },
           forkAt: (seq) => {
             ctx.sessions.fork({ sessionId, atSeq: seq, increaseTitle: true })
               .then((childId) => { ctx.sessions.open(childId) })
diff --git a/packages/client/ui-chat/src/client/chat/ChatView.tsx b/packages/client/ui-chat/src/client/chat/ChatView.tsx
index 403eecd..d6713f7 100644
--- a/packages/client/ui-chat/src/client/chat/ChatView.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatView.tsx
@@ -217,6 +217,7 @@ const ChatNodeList = memo(function ChatNodeList({ order, ...seatProps }: ChatNod
 export function ChatView({
   useSession, useChat, useChatNode, useChatNodeProcess, useSessions, useStore, actions, renderSlot,
   sessionId, openFile, loadOlder, loadThrough, loadImage, openView, chatScroll, forkAt, fileMentions,
+  inspectHandoff,
   useTranscriptView, useProjection, t,
 }: ChatViewSlotProps) {
   const order = useChat(s => s.order)
@@ -244,8 +245,14 @@ export function ChatView({
   const selectedCallId = useStore(s => s.selection?.callId)
   const compactTranscript = useTranscriptView(mode => mode === 'compact')
   const inspectCall = useCallback((callId: string) => {
+    // patches/0008: the desktop panel service, when present, takes the
+    // inspect handoff wholesale (opens the panel column, activates the
+    // trajectory page, delivers the one-shot target). Lazy like fileMentions:
+    // composing the panel shell in or out takes effect live. Unregistered
+    // 'trajectory' id is safe: the tab ring falls back to the first view.
+    if (inspectHandoff?.(callId) === true) return
     openView('trajectory', callId)
-  }, [openView])
+  }, [inspectHandoff, openView])
   const [fileOpenError, setFileOpenError] = useState<{ path: string; message: string } | null>(null)
   const [fileOpenBusy, setFileOpenBusy] = useState(false)
   // Close/retry must ignore a settlement that started before the latest
diff --git a/packages/client/ui-chat/src/client/contract/slots.ts b/packages/client/ui-chat/src/client/contract/slots.ts
index 16daf32..396687c 100644
--- a/packages/client/ui-chat/src/client/contract/slots.ts
+++ b/packages/client/ui-chat/src/client/contract/slots.ts
@@ -144,6 +144,14 @@ export interface ChatViewInjected {
   }
   forkAt: (seq: number) => void
   fileMentions: (owner: TurnTailOwnerProps) => MarkdownFileMentions | undefined
+  /**
+   * Desktop panel handoff for Inspect (patches/0008): returns true when the
+   * panel shell consumed the call (opened the panel column, activated the
+   * trajectory page, and delivered the one-shot inspect target itself). The
+   * panel column lives outside this conversation subtree, so the chat-store
+   * inspect target below is unreachable from there.
+   */
+  inspectHandoff?: (callId: string) => boolean
 }
 
 /** Full Chat view props. */
