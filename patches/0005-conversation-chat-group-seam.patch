diff --git a/packages/client/ui-chat/src/client/apply.ts b/packages/client/ui-chat/src/client/apply.ts
index 9c41407..3147805 100644
--- a/packages/client/ui-chat/src/client/apply.ts
+++ b/packages/client/ui-chat/src/client/apply.ts
@@ -100,6 +100,7 @@ export function apply(ctx: Context): void {
       locale: NS,
       children: {
         'conversation.chat.node': { kind: 'keyed', scope: 'session', inject: CHAT_NODE_INJECT },
+        'conversation.chat.group': { kind: 'single', scope: 'session' },
         'conversation.message.images': { kind: 'single', scope: 'session' },
       },
       store: chatStore,
@@ -119,6 +120,10 @@ export function apply(ctx: Context): void {
             ctx.layout.openDetails()
           },
           fileMentions: (owner: TurnTailOwnerProps) => ctx.get('chatFileMentions')?.forClosing(owner),
+          // Lazy like fileMentions: the grouping plugin may compose in or out
+          // around this registration, so the service resolves per read
+          // (patches/0005).
+          flowGrouping: () => ctx.get('chatFlowGrouping'),
           openFile: async (path) => {
             // patches/0009: the desktop file-browser seam gets the first look
             // (tree/preview opens inside the panel). Directories and paths the
diff --git a/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx b/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
index c517f9d..0c88ea6 100644
--- a/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
+++ b/packages/client/ui-chat/src/client/chat/AssistantMarkdown.tsx
@@ -2,7 +2,7 @@ import { Fragment, memo, useMemo } from 'react'
 import type { ReactNode } from 'react'
 import { JsonBlock, MarkdownText } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { MarkdownFileMentions } from '@deepseek-ai/dsh-client-ui-primitives'
-import type { ChatNodeOwnerProps, ChatViewSlotProps } from '../contract/slots.ts'
+import type { ChatNodeOwnerProps, ChatNodeRenderVariant, ChatViewSlotProps } from '../contract/slots.ts'
 import type { AssistantBlock } from '../contract/snapshot.ts'
 import { markdownLabels } from '../markdown-labels.ts'
 import { ReasoningRow } from './ReasoningRow.tsx'
@@ -10,6 +10,7 @@ import { useSearchableHidden } from './searchable-hidden.ts'
 import css from './AssistantMarkdown.module.css'
 
 export interface AssistantMarkdownProps {
+  variant?: ChatNodeRenderVariant | undefined
   blocks: readonly AssistantBlock[]
   streaming: boolean
   /** Frozen partial of an aborted turn: rendered with a stopped marker. */
@@ -28,7 +29,7 @@ export interface AssistantMarkdownProps {
 
 /** Reasoning block as the Think variant summary row (figma 39:28304). */
 export const AssistantMarkdown = memo(function AssistantMarkdown({
-  blocks, streaming, interrupted, renderMessageImages,
+  blocks, variant, streaming, interrupted, renderMessageImages,
   reasoningHidden = false, revealProcess, mentions, t,
 }: AssistantMarkdownProps) {
   // Stable per locale revision (t identity changes on switch): a fresh object
@@ -38,14 +39,17 @@ export const AssistantMarkdown = memo(function AssistantMarkdown({
   // Tool-call heads render as tool rows in the chat view's grouping pass, so
   // a node that is only those heads (or empty) would paint an empty root
   // between tool groups — skip the shell unless something visible remains.
-  const hasVisible = streaming
-    || interrupted === true
-    || blocks.some(block => block.kind !== 'tool-call')
+  const includes = (block: AssistantBlock): boolean => variant === 'prose-only'
+    ? block.kind !== 'reasoning'
+    : variant === 'reasoning-only' ? block.kind === 'reasoning' : true
+  const hasVisible = (streaming && variant !== 'reasoning-only')
+    || (interrupted === true && variant !== 'reasoning-only')
+    || blocks.some(block => includes(block) && block.kind !== 'tool-call')
   if (!hasVisible) return null
   const rendered: ReactNode[] = []
   for (let i = 0; i < blocks.length; i++) {
     const block = blocks[i]
-    if (block === undefined) continue
+    if (block === undefined || !includes(block)) continue
     switch (block.kind) {
       case 'text':
         rendered.push(
@@ -111,7 +115,7 @@ export const AssistantMarkdown = memo(function AssistantMarkdown({
     <div className={css.root} data-streaming={streaming || undefined}>
       <div className={css.body}>
         {rendered}
-        {interrupted && <span className={css.stopped}>{t('message.stopped')}</span>}
+        {interrupted && variant !== 'reasoning-only' && <span className={css.stopped}>{t('message.stopped')}</span>}
       </div>
     </div>
   )
diff --git a/packages/client/ui-chat/src/client/chat/AssistantNodeView.tsx b/packages/client/ui-chat/src/client/chat/AssistantNodeView.tsx
index 399897a..db138ce 100644
--- a/packages/client/ui-chat/src/client/chat/AssistantNodeView.tsx
+++ b/packages/client/ui-chat/src/client/chat/AssistantNodeView.tsx
@@ -4,7 +4,7 @@ import { AssistantMarkdown } from './AssistantMarkdown.tsx'
 
 /** Streaming, settled, and interrupted Assistant states share one keyed renderer instance. */
 export const AssistantNodeView = memo(function AssistantNodeView({
-  node, useTurnData, turnProcess, openFile, renderMessageImages, fileMentions, t,
+  node, renderVariant, useTurnData, turnProcess, openFile, renderMessageImages, fileMentions, t,
 }: ChatNodeViewProps<'assistant-step'>) {
   const data = node.data
   const turn = node.location.kind === 'turn' || node.location.kind === 'step'
@@ -29,6 +29,7 @@ export const AssistantNodeView = memo(function AssistantNodeView({
   return (
     <AssistantMarkdown
       blocks={data.blocks}
+      variant={renderVariant}
       streaming={data.status === 'running'}
       interrupted={data.status === 'interrupted'}
       renderMessageImages={renderMessageImages}
diff --git a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
index 4f51486..85db170 100644
--- a/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatNodeSeat.tsx
@@ -1,7 +1,9 @@
-import { memo, useCallback, useMemo } from 'react'
+import { memo, useCallback, useMemo, type ReactNode } from 'react'
 import { JsonBlock } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { ConversationLocationDataStore, ConversationTurnDataMap } from '@deepseek-ai/dsh-client-ui-conversation/client'
-import type { ChatNodeOwnerProps, ChatViewSlotProps } from '../contract/slots.ts'
+import type {
+  ChatNodeOwnerProps, ChatNodeRenderVariant, ChatViewSlotProps,
+} from '../contract/slots.ts'
 import type { ChatNode } from '../contract/chat-nodes.ts'
 import { TURN_PROCESS_INDEPENDENT_KINDS } from '../contract/turn-process.ts'
 import { storedTurnProcessEntry } from '../stores.ts'
@@ -9,7 +11,11 @@ import { useSearchableHidden } from './searchable-hidden.ts'
 import css from './ChatView.module.css'
 
 interface ChatNodeSeatProps extends ChatNodeOwnerProps {
+  readonly children?: ReactNode
+  readonly flowKind?: string
   readonly nodeKey: string
+  /** Optional block-range variant threaded into the owner share. */
+  readonly renderVariant?: ChatNodeRenderVariant | undefined
   readonly useChatNode: ChatViewSlotProps['useChatNode']
   readonly useChatNodeProcess: ChatViewSlotProps['useChatNodeProcess']
   readonly historyIncomplete: boolean
@@ -36,7 +42,7 @@ function turnOf(node: ChatNode | undefined): number | undefined {
 
 /** Subscribe, apply Turn-process visibility, and dispatch one stable Context key. */
 export const ChatNodeSeat = memo(function ChatNodeSeat({
-  nodeKey, useChatNode, useChatNodeProcess, historyIncomplete, compactTranscript,
+  nodeKey, children, flowKind, renderVariant, useChatNode, useChatNodeProcess, historyIncomplete, compactTranscript,
   selectedCallId, cwd, openFile, inspectCall, forkAt,
   renderMessageImages, fileMentions, useStore, actions, renderSlot, t,
 }: ChatNodeSeatProps) {
@@ -110,10 +116,11 @@ export const ChatNodeSeat = memo(function ChatNodeSeat({
       forkAt,
       renderMessageImages,
       fileMentions,
+      ...renderVariant === undefined ? {} : { renderVariant },
       turnProcess,
     }, [
     node, selectedCallId, cwd, openFile, inspectCall, forkAt,
-    renderMessageImages, fileMentions, turnProcess,
+    renderMessageImages, fileMentions, renderVariant, turnProcess,
   ])
   if (routedNode === undefined || owner === null) return null
   const turnData = turnDataOf(routedNode)
@@ -127,13 +134,13 @@ export const ChatNodeSeat = memo(function ChatNodeSeat({
       className={css.flowItem}
       data-chat-anchor-key={routedNode.key}
       data-chat-flow-key={routedNode.key}
-      data-chat-flow-kind={routedNode.kind}
+      data-chat-flow-kind={flowKind ?? routedNode.kind}
       data-chat-turn={turn}
       data-turn-process-member={processMember || undefined}
       data-turn-process-hidden={processHidden || undefined}
       data-turn-process-answer={compactAnswer || undefined}
     >
-      {renderSlot('conversation.chat.node', routedOwner, {
+      {children ?? renderSlot('conversation.chat.node', routedOwner, {
         entryKey: routedNode.kind,
         hookContext: turnData,
         fallback: (
diff --git a/packages/client/ui-chat/src/client/chat/ChatView.tsx b/packages/client/ui-chat/src/client/chat/ChatView.tsx
index d6713f7..5170d0c 100644
--- a/packages/client/ui-chat/src/client/chat/ChatView.tsx
+++ b/packages/client/ui-chat/src/client/chat/ChatView.tsx
@@ -1,3 +1,4 @@
+import type { ChatNode } from '../contract/chat-nodes.ts'
 // An enclosing `[data-conversation-scroll]` owns scrolling when present;
 // otherwise this view owns it. Each row subscribes to one stable node key.
 
@@ -7,7 +8,9 @@ import type {
 } from '@deepseek-ai/dsh-client-ui-conversation/client'
 import type { SessionSeq } from '@deepseek-ai/dsh-session/types'
 import { Button, IconChevronDownOutline14, Modal } from '@deepseek-ai/dsh-client-ui-primitives'
-import type { ChatViewSlotProps } from '../contract/slots.ts'
+import type {
+  ChatFlowItem, ChatNodeRenderVariant, ChatViewSlotProps,
+} from '../contract/slots.ts'
 import type { ChatSnapshot } from '../contract/snapshot.ts'
 import { PendingSteeringBubble, PendingSubmissionBubble } from './MessageItem.tsx'
 import { ChatNodeSeat } from './ChatNodeSeat.tsx'
@@ -201,13 +204,38 @@ function TurnStatus({ startTime, t }: {
 }
 
 type ChatNodeListProps = Omit<ComponentProps<typeof ChatNodeSeat>, 'nodeKey'> & {
-  readonly order: readonly string[]
+  readonly flowItems: readonly ChatFlowItem[]
 }
 
-const ChatNodeList = memo(function ChatNodeList({ order, ...seatProps }: ChatNodeListProps) {
-  return order.map(nodeKey => (
-    <ChatNodeSeat key={nodeKey} nodeKey={nodeKey} {...seatProps} />
-  ))
+const ChatNodeList = memo(function ChatNodeList({ flowItems, ...seatProps }: ChatNodeListProps) {
+  // One stable seat renderer per node key, shared by the flat and grouped
+  // flows (patches/0005): the grouped flow's renderMember hands members back
+  // through this exact path, so an expanded group is the flattened
+  // transcript. The optional variant splits an Assistant step's blocks for
+  // folding groups (reasoning half inside the group, prose half outside).
+  const renderSeat = (nodeKey: string, variant?: ChatNodeRenderVariant) => (
+    <ChatNodeSeat key={nodeKey} nodeKey={nodeKey} renderVariant={variant} {...seatProps} />
+  )
+  return flowItems.map(item => item.kind === 'group'
+    ? (
+      // The group shares its first member's native process visibility and
+      // searchable-hidden boundary; a bare div would leak its summary when
+      // the enclosing Turn process is closed.
+      <ChatNodeSeat
+        key={item.key}
+        nodeKey={item.key}
+        flowKind="activity-group"
+        {...seatProps}
+      >
+        {seatProps.renderSlot('conversation.chat.group', {
+          nodes: item.nodes,
+          renderMember: (memberKey, variant) => renderSeat(memberKey, variant),
+        }, {
+          fallback: item.nodes.map(member => renderSeat(member.key)),
+        })}
+      </ChatNodeSeat>
+    )
+    : renderSeat(item.key))
 })
 
 /**
@@ -218,10 +246,46 @@ export function ChatView({
   useSession, useChat, useChatNode, useChatNodeProcess, useSessions, useStore, actions, renderSlot,
   sessionId, openFile, loadOlder, loadThrough, loadImage, openView, chatScroll, forkAt, fileMentions,
   inspectHandoff,
+  flowGrouping,
   useTranscriptView, useProjection, t,
 }: ChatViewSlotProps) {
-  const order = useChat(s => s.order)
-  const nodeStore = useChat(s => s.nodes)
+  const chat = useChat(s => s)
+  const { order, nodes: nodeStore } = chat
+  // Flow assembly (patches/0005): the ordered visible nodes, optionally
+  // reassembled by the optional grouping service into node/group alternation.
+  // Service resolution stays lazy so composing the provider in or out takes
+  // effect live; node references come from the same keyed store the seats
+  // subscribe to, so item identity follows projection reference stability.
+  const flowItems = useMemo<readonly ChatFlowItem[]>(() => {
+    const nodes: ChatNode[] = []
+    for (const key of order) {
+      const node = nodeStore.get(key)
+      if (node !== undefined) nodes.push(node as ChatNode)
+    }
+    const assembler = flowGrouping()
+    if (assembler === undefined) {
+      return nodes.map(node => ({ kind: 'node' as const, key: node.key, node }))
+    }
+    const items: ChatFlowItem[] = []
+    let process: ChatNode[] = []
+    const flush = (): void => {
+      items.push(...assembler.group(process))
+      process = []
+    }
+    for (const node of nodes) {
+      const spec = nodeStore.processSource(node.key).getSnapshot()?.spec
+      // Never let a group led by a hidden process member swallow the final
+      // answer. Its own Seat owns inline reasoning and answer presentation.
+      if (node.kind === 'assistant-step' && spec?.answerStep === node.data.step) {
+        flush()
+        items.push({ kind: 'node', key: node.key, node })
+      } else {
+        process.push(node)
+      }
+    }
+    flush()
+    return items
+  }, [chat, flowGrouping])
   // The rail's items are accumulated in the Chat snapshot, so this selector is
   // both the data and its change signal: the array identity moves only when a
   // Turn enters, leaves, or changes its preview.
@@ -782,7 +846,7 @@ export function ChatView({
             </div>
           )}
           <ChatNodeList
-            order={order}
+            flowItems={flowItems}
             useChatNode={useChatNode}
             useChatNodeProcess={useChatNodeProcess}
             historyIncomplete={hasMore}
diff --git a/packages/client/ui-chat/src/client/contract/slots.ts b/packages/client/ui-chat/src/client/contract/slots.ts
index 396687c..dacd130 100644
--- a/packages/client/ui-chat/src/client/contract/slots.ts
+++ b/packages/client/ui-chat/src/client/contract/slots.ts
@@ -1,3 +1,4 @@
+import type { ReactNode } from 'react'
 /** Chat-owned Slot declarations and composed component props. */
 import type { MessageId } from '@deepseek-ai/dsh-llm/brand'
 import type { SessionSeq } from '@deepseek-ai/dsh-session/types'
@@ -78,6 +79,12 @@ export interface ChatNodeOwnerProps {
   inspectCall: (callId: ToolCallId) => void
   forkAt: (seq: number) => void
   renderMessageImages: RenderMessageImages
+  /**
+   * Block-range variant of this seat's Assistant rendering (see
+   * {@link ChatNodeRenderVariant}); absent = the unfiltered step. Only the
+   * chat flow's folding groups pass it, through `renderMember`.
+   */
+  renderVariant?: ChatNodeRenderVariant | undefined
   fileMentions: (owner: TurnTailOwnerProps) => MarkdownFileMentions | undefined
   /** Turn-process state when this Node belongs to a projected Turn. */
   turnProcess?: TurnProcessOwnerProps | undefined
@@ -152,12 +159,76 @@ export interface ChatViewInjected {
    * inspect target below is unreachable from there.
    */
   inspectHandoff?: (callId: string) => boolean
+  /**
+   * Lazy read of the optional {@link ChatFlowGrouping} assembler (resolved
+   * per call like {@link ChatViewInjected.fileMentions}, so composing the
+   * provider in or out takes effect live). Undefined = the view renders the
+   * plain flattened transcript.
+   */
+  flowGrouping: () => ChatFlowGrouping | undefined
+}
+
+
+/**
+ * One item of the chat view's assembled flow: either a business node rendered
+ * as its own seat, or a synthetic group folding consecutive process-only
+ * nodes (reasoning-only Assistant steps, tool calls) behind one summary row.
+ * A group's `key` is its first member's node key, so scroll anchoring and
+ * flow identity stay expressible without new identity machinery.
+ */
+export type ChatFlowItem =
+  | { readonly kind: 'node'; readonly key: string; readonly node: ChatNode }
+  | { readonly kind: 'group'; readonly key: string; readonly nodes: readonly ChatNode[] }
+
+/**
+ * Block-range rendering variant of one Assistant step's seat. Folding groups
+ * split a prose-tailed step (reasoning followed by the answer's text): the
+ * reasoning half renders inside the group, the prose half outside it, and
+ * the unfiltered seat stays the default. Absent variant = every block.
+ */
+export type ChatNodeRenderVariant = 'prose-only' | 'reasoning-only'
+
+/**
+ * Owner currency of one folded group's summary row.
+ */
+export interface ChatFlowGroupOwnerProps {
+  /** Folded member nodes in ascending anchorSeq order. */
+  nodes: readonly ChatNode[]
+  /**
+   * Render one member with the exact flattened seat — anchor key, flow
+   * wrapper, and keyed dispatch identical to the unfolded view. The optional
+   * variant splits a member's blocks: a folding group renders a prose-tailed
+   * step's reasoning half inside itself and its prose half outside.
+   */
+  renderMember: (nodeKey: string, variant?: ChatNodeRenderVariant) => ReactNode
+}
+
+/**
+ * Optional chat flow assembler, consumed via `ctx.get('chatFlowGrouping')`
+ * (same optional-service convention as {@link ChatFileMentions}): the chat
+ * view hands it the visible business nodes in ascending anchorSeq order and
+ * renders the returned sequence instead of the flat one. Absent service is
+ * the off state: the view renders the plain flattened transcript. The service
+ * owns only assembly; the group summary row itself renders through the
+ * `conversation.chat.group` slot, and the two registrations belong to the
+ * same plugin so they compose together.
+ */
+export interface ChatFlowGrouping {
+  /**
+   * Reassemble the ordered node sequence into nodes and folded groups.
+   * Implementations must be pure projections: they may reorder, skip, or
+   * fold nodes into groups, but every member stays addressable through its
+   * group so no row can be lost from the transcript.
+   * @param nodes - Visible business nodes in ascending anchorSeq order.
+   * @returns The assembled flow items in render order.
+   */
+  group(nodes: readonly ChatNode[]): readonly ChatFlowItem[]
 }
 
 /** Full Chat view props. */
 export type ChatViewSlotProps =
   PropsRuntime<'conversation.view'>
-  & PropsRenderSlots<'conversation.chat.node' | 'conversation.message.images'>
+  & PropsRenderSlots<'conversation.chat.node' | 'conversation.chat.group' | 'conversation.message.images'>
   & PropsStore<ChatStore>
   & InjectFace<ChatViewInjected>
   & PropsLocale<'chat'>
@@ -203,6 +274,17 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
       hookContext: ConversationLocationDataStore<ConversationTurnDataMap> | undefined
       inject: ChatNodeTurnDataInjected
     }
+    /**
+     * The summary row of one folded activity group produced by the optional
+     * {@link ChatFlowGrouping} assembly service (patches/0005). Declared by
+     * the chat view entry; the owner passes the folded member nodes (ascending
+     * anchorSeq) and a `renderMember` that renders one member with the exact
+     * flattened seat, so an expanded group is visually and behaviorally the
+     * flattened flow. Unoccupied, the view expands every group through that
+     * same fallback — a grouping service without a renderer degrades to the
+     * flattened transcript, never to lost rows.
+     */
+    'conversation.chat.group': { kind: 'single'; scope: 'session'; owner: ChatFlowGroupOwnerProps }
     /**
      * Renderer for one consecutive group of durable message images. The owner
      * supplies image references, an authorized loader, and alignment. A
diff --git a/packages/client/ui-chat/src/client/index.ts b/packages/client/ui-chat/src/client/index.ts
index 5cae4dc..05ed5db 100644
--- a/packages/client/ui-chat/src/client/index.ts
+++ b/packages/client/ui-chat/src/client/index.ts
@@ -31,7 +31,8 @@ export type { ChatStoreState, SelectionTarget, ToolCallId, TurnProcessViewEntry
 export type { TranscriptViewRowInjected, TranscriptViewRowProps } from './settings/TranscriptViewRow.tsx'
 export type { TranscriptViewMode } from '../chat-settings.ts'
 export type {
-  AssistantActionOwnerProps, ChatFileMentions, ChatNodeOwnerProps, ChatNodeTurnDataInjected,
+  AssistantActionOwnerProps, ChatFileMentions, ChatFlowGroupOwnerProps, ChatFlowGrouping, ChatFlowItem,
+  ChatNodeOwnerProps, ChatNodeRenderVariant, ChatNodeTurnDataInjected,
   ChatNodeViewProps, ChatScrollPosition, ChatStore, ChatViewInjected, ChatViewSlotProps,
   CommandRowOwnerProps, CommandRowProps, DetailsInjected, DetailsSlotProps,
   DetailsToolOwnerProps, MessageImagesProps,
diff --git a/packages/client/ui-chat/tests/chat-view.client.spec.tsx b/packages/client/ui-chat/tests/chat-view.client.spec.tsx
index 9e0818f..06134ad 100644
--- a/packages/client/ui-chat/tests/chat-view.client.spec.tsx
+++ b/packages/client/ui-chat/tests/chat-view.client.spec.tsx
@@ -401,6 +401,7 @@ function makeHarness(
     forkAt,
     // Absent-service default; mention tests override with a real resolver.
     fileMentions: () => undefined,
+    flowGrouping: () => undefined,
     t,
   }
   const set = (next: HarnessUpdate): void => {
@@ -1263,6 +1264,60 @@ describe('ChatView', () => {
     expect(branchButtons.map(button => button.getAttribute('aria-disabled'))).toEqual([null, null])
   })
 
+  it('folds activity-group summaries with the native process and keeps the final answer outside', () => {
+    const first = { ...assistant(2, '', 1, 1), blocks: [{ kind: 'reasoning' as const, text: 'inspect' }] }
+    const answer = { ...assistant(5, 'final answer', 1, 2), blocks: [
+      { kind: 'reasoning' as const, text: 'conclude' },
+      { kind: 'text' as const, text: 'final answer' },
+    ] }
+    const h = makeHarness({
+      nodes: [user(1, 'question'), first, toolResult(3, 'a'), toolResult(4, 'b'), answer],
+      turnTimings: new Map([[1, { startTime: 1_000, endTime: 5_000 }]]),
+      turnEnds: new Map([[1, 6]]),
+    })
+    h.props.flowGrouping = () => ({ group: nodes => {
+      const items: import('../src/client/contract/slots.ts').ChatFlowItem[] = []
+      let run: ChatNode[] = []
+      const flush = (): void => {
+        if (run.length > 1) items.push({ kind: 'group', key: run[0]!.key, nodes: run })
+        else for (const node of run) items.push({ kind: 'node', key: node.key, node })
+        run = []
+      }
+      for (const node of nodes) {
+        if (node.kind === 'assistant-step' || node.kind === 'tool-call') run.push(node)
+        else { flush(); items.push({ kind: 'node', key: node.key, node }) }
+      }
+      flush()
+      return items
+    } })
+    const original = h.props.renderSlot
+    h.props.renderSlot = ((key: string, owner: object, opts: unknown) => {
+      if (key === 'conversation.chat.group') {
+        const group = owner as import('../src/client/contract/slots.ts').ChatFlowGroupOwnerProps
+        return <div><span>activity summary</span>{group.nodes.map(node => group.renderMember(node.key))}</div>
+      }
+      return (original as (key: string, owner: object, opts: unknown) => React.ReactNode)(key, owner, opts)
+    }) as typeof original
+    const view = render(<h.ChatView {...h.props} />)
+    const group = view.container.querySelector<HTMLElement>('[data-chat-flow-kind="activity-group"]')!
+    const toggle = view.container.querySelector<HTMLButtonElement>('[data-turn-process-tool-calls]')!
+    expect(group.getAttribute('hidden')).toBe('until-found')
+    expect(group.textContent).toContain('activity summary')
+    expect(group.textContent).not.toContain('final answer')
+    expect(view.getByText('final answer').closest('[hidden]')).toBeNull()
+    fireEvent.click(toggle)
+    expect(group.hasAttribute('hidden')).toBe(false)
+    expect(group.textContent).toContain('bash:a')
+    fireEvent.click(toggle)
+    expect(group.getAttribute('hidden')).toBe('until-found')
+    fireEvent(group, new Event('beforematch'))
+    expect(group.hasAttribute('hidden')).toBe(false)
+    act(() => { h.setTranscriptView('normal') })
+    expect(group.hasAttribute('hidden')).toBe(false)
+    act(() => { h.setTranscriptView('compact'); h.setSession({ hasMore: true }) })
+    expect(group.hasAttribute('hidden')).toBe(false)
+  })
+
   it('folds Think and Tool rows before the final answer without unmounting them', () => {
     const first = {
       ...assistant(2, 'earlier reply', 1, 1),
diff --git a/packages/client/ui-chat/tests/image-labels.client.spec.tsx b/packages/client/ui-chat/tests/image-labels.client.spec.tsx
index ffbde6b..6689ed6 100644
--- a/packages/client/ui-chat/tests/image-labels.client.spec.tsx
+++ b/packages/client/ui-chat/tests/image-labels.client.spec.tsx
@@ -97,3 +97,21 @@ describe('assistant image slot handoff', () => {
     expect(image.compareDocumentPosition(after) & Node.DOCUMENT_POSITION_FOLLOWING).not.toBe(0)
   })
 })
+
+
+it('splits intermediate reasoning and prose without duplicate text or images', () => {
+  const calls: MessageImagesRenderOwner[] = []
+  const blocks = [
+    { kind: 'reasoning' as const, text: 'Internal plan' },
+    { kind: 'text' as const, text: 'Intermediate result' },
+    { kind: 'image' as const, attachment },
+  ]
+  const view = render(<>
+    <AssistantMarkdown t={t} blocks={blocks} variant="reasoning-only" streaming={false} renderMessageImages={imageRenderer(calls)} />
+    <AssistantMarkdown t={t} blocks={blocks} variant="prose-only" streaming={false} renderMessageImages={imageRenderer(calls)} />
+  </>)
+  expect(view.getAllByText('Intermediate result')).toHaveLength(1)
+  expect(view.getAllByText('Internal plan')).toHaveLength(1)
+  expect(calls).toHaveLength(1)
+  expect(view.getAllByTestId('message-images')).toHaveLength(1)
+})
