diff --git a/packages/client/ui-layout/src/client/AppFrame.module.css b/packages/client/ui-layout/src/client/AppFrame.module.css
index ae56780133..e119aebc15 100644
--- a/packages/client/ui-layout/src/client/AppFrame.module.css
+++ b/packages/client/ui-layout/src/client/AppFrame.module.css
@@ -4,6 +4,15 @@
   grid-template-rows: 100%;
   height: 100%;
   overflow: hidden;
+  /* Inline-size container for the fullscreen panel overlay: the overlay's
+     target width is expressed as `calc(100cqi - sidebarTrack)` so BOTH the
+     column and its interior pin resolve against the frame's own width —
+     never against each other's animated box. (Without it, the pin's `100%`
+     reads the panelCol it sits in and the sidebar gets subtracted twice.)
+     Side effect: fixed-position descendants would anchor here; every fixed
+     surface in the app is portaled to body or lives in a full-bleed frame
+     whose box equals the viewport, so the containment is visually inert. */
+  container-type: inline-size;
   background: var(--dsw-alias-bg-base);
   /* Collapse/expand animates the tracks on the deepsuite sider curve
      (--ds-ease-in-out / --ds-transition-duration-slow, ui-theme base.css). */
@@ -48,6 +57,81 @@
   border-left: none;
 }
 
+/* The conversation column contains its own elevated layers (the input dock
+   rides z-index above the message flow, the context meter higher still).
+   Without a local stacking context those values compete frame-wide and the
+   dock floats over the fullscreen panel overlay. isolation keeps every
+   internal z-index local: the overlay is a frame-level sibling, so it
+   covers the column no matter what its children raise. */
+.centerCol,
+.detailsCol {
+  isolation: isolate;
+}
+
+/* The panel column, with the left stroke dividing it from the CENTER column.
+   position: relative anchors the interior pin (panelColPin) to THIS column —
+   dropping it lets the pin anchor to the whole frame and the panel renders
+   over the window's left edge. The stroke rides the column's own left edge,
+   so it is present for the whole eased track motion instead of popping in
+   when the reveal completes. Collapsed suppresses it (a 0-width column would
+   still paint it); the overlay presentation suppresses it as well (see
+   data-panel-overlay below). */
+.panelCol {
+  position: relative;
+  min-width: 0;
+  overflow: hidden;
+  background: var(--dsw-alias-bg-base);
+  border-left: 1px solid var(--dsw-alias-border-l2);
+}
+
+.frame[data-panel-collapsed] .panelCol {
+  border-left: none;
+}
+
+/* Fullscreen overlay presentation. data-panel-overlay is the presentation
+   flag — it outlives data-panel-expanded for the length of the restore ease
+   (the render releases it on the width transition end). The column leaves
+   the flow, pins to the window's right edge, and eases its width over the
+   untouched center: zero reflow of the covered columns, the interior is
+   pinned at the target width, so the animation only moves a clip box.
+   z-index sits above the columns but below the drag handles (the sidebar
+   handle stays usable at the overlay's left edge; the details and panel
+   handles are suppressed in the render for the whole episode). The left
+   stroke stays suppressed: at full width the overlay adjoins the sidebar,
+   whose seam is a fill change by convention — never a stroke. */
+.frame[data-panel-overlay] .panelCol {
+  position: absolute;
+  top: 0;
+  right: 0;
+  bottom: 0;
+  z-index: 1;
+  border-left: none;
+  transition: width var(--ds-transition-duration-slow) var(--ds-ease-in-out);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .frame[data-panel-overlay] .panelCol {
+    /* Duration 0, not `transition: none`: the transition must still exist
+       so its end event releases the overlay presentation after the snap. */
+    transition-duration: 0s;
+  }
+}
+
+/* The panel interior, anchored to the column's LEFT edge at its target
+   width. Anchoring left (not right) makes the interior's position derive
+   from the same layout pass as the eased track: it slides with the moving
+   seam in lockstep — one animated property, no compositor/main-thread
+   desync — while its fixed width keeps the content from ever re-wrapping.
+   The column's overflow clips the part that still lies beyond the window
+   edge mid-motion. On release the column width equals the pin width, so
+   re-joining the flow is seamless. */
+.panelColPin {
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  left: 0;
+}
+
 /* Drag handles are frame children (columns clip overflow): an 8px hit strip
    centered on the column border via inline left, above column content. Details
    adds a visible 12x32 pill at vertical center; sidebar keeps only the hit strip. */
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index 2696dc91fa..2c07e096c6 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -1,16 +1,19 @@
 /**
- * Three-column shell frame, registered into the built-in 'root' slot (the web
- * shell renders only 'root'). Owns the grid tracks (sidebar | center |
- * details), the drag handles (pointer capture + rAF throttle), the concession
- * chain (columns.ts), and the child-slot render decisions: the sidebar slot
- * renders HERE with live parameters from the concession solve, and the
- * session-aware occupants render in fixed column positions; strict entries
- * gate themselves on current-session availability while session-maybe
- * entries retain identity. Pure component: everything arrives
+ * Three-column-plus-panel shell frame, registered into the built-in 'root'
+ * slot (the web shell renders only 'root'). Owns the grid tracks (sidebar |
+ * center | details | panel), the drag handles (pointer capture + rAF
+ * throttle), the concession chain (columns.ts), and the child-slot render
+ * decisions: the sidebar slot renders HERE with live parameters from the
+ * concession solve, and the session-aware occupants render in fixed column
+ * positions; strict entries gate themselves on current-session availability
+ * while session-maybe entries retain identity. Panel open/close ride the
+ * frame's own eased grid-track transition (the push-panel standard; dragging
+ * suspends the easing); fullscreen is an overlay that covers the content
+ * without ever re-wrapping it. Pure component: everything arrives
  * through the three framework shares — zero cordis or framework imports,
  * zero self-made hooks.
  */
-import { useCallback, useEffect, useLayoutEffect, useRef, useState } from 'react'
+import { memo, useCallback, useEffect, useLayoutEffect, useRef, useState } from 'react'
 import type { ReactNode } from 'react'
 import type { PropsRenderSlots, PropsRuntime, PropsStore } from '@deepseek-ai/dsh-client-ui-slots'
 import { computeColumns, SIDEBAR_AUTO_COLLAPSE, SIDEBAR_DEFAULT } from './columns.ts'
@@ -20,9 +23,26 @@ import css from './AppFrame.module.css'
 /** Full composed props: runtime share + child-slot render share + store share. */
 export type AppFrameProps =
   & PropsRuntime<'root'>
-  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'shell.overlay'>
+  & PropsRenderSlots<'sidebar' | 'conversation' | 'details' | 'panel' | 'shell.overlay'>
   & PropsStore<ReturnType<typeof createLayoutStore>>
 
+type StaticSlotKey = 'conversation' | 'details' | 'panel' | 'shell.overlay'
+
+/**
+ * Layout drag/transition ticks only change frame geometry. The slot dispatcher
+ * and every static occupant below it have stable identities, so keep them
+ * behind one memo boundary instead of rebuilding those subtrees per tick.
+ */
+const StaticSlot = memo(function StaticSlot({
+  renderSlot,
+  slotKey,
+}: {
+  renderSlot: AppFrameProps['renderSlot']
+  slotKey: StaticSlotKey
+}) {
+  return renderSlot(slotKey, {})
+})
+
 /** Center column grid item (session-body building block). */
 function CenterColumn(props: { children?: ReactNode }) {
   return <div className={css.centerCol}>{props.children}</div>
@@ -37,7 +57,7 @@ function DetailsColumn(props: { children?: ReactNode }) {
  * One drag handle: pointer capture, rAF-throttled dx reports against the drag-start origin.
  * `side` keys the hover-reveal CSS to the owning column.
  */
-function DragHandle(props: { side: 'sidebar' | 'details'; left: number; onStart: () => void; onDrag: (dx: number) => void; onEnd: () => void }) {
+function DragHandle(props: { side: 'sidebar' | 'details' | 'panel'; left: number; onStart: () => void; onDrag: (dx: number) => void; onEnd: () => void }) {
   const [dragging, setDragging] = useState(false)
   const origin = useRef(0)
   const latest = useRef(0)
@@ -139,35 +159,103 @@ export function AppFrame({
   const sidebarPreference = sidebarCollapsed
     ? 0
     : panels.sidebar === 0 ? SIDEBAR_DEFAULT : panels.sidebar
-  const cols = computeColumns(viewport, sidebarPreference, detailsSession === undefined ? 0 : panels.details)
-  const colsRef = useRef(cols)
-  colsRef.current = cols
+  const cols = computeColumns(
+    viewport,
+    sidebarPreference,
+    detailsSession === undefined ? 0 : panels.details,
+    panels.panel,
+  )
+  // Panel fullscreen (focus mode): an OVERLAY presentation. The grid tracks
+  // are untouched — the center gets covered, never re-wrapped (zero layout
+  // work per animation frame) — and the panel column leaves the flow at an
+  // inline target width (CSS anchors on data-panel-overlay). `panelExpanded`
+  // is the store intent (the titlebar icon flips immediately); `overlay` is
+  // the presentation, which outlives the intent on restore so the width can
+  // ease back down before the column rejoins the flow at the same geometry.
+  const panelExpanded = panels.panelExpanded && cols.panel > 0
+  const [overlay, setOverlay] = useState(false)
+  useEffect(() => {
+    if (panelExpanded) setOverlay(true)
+  }, [panelExpanded])
+  // Belt-and-braces release: the presentation normally drops on the width
+  // transition end; if that event is ever lost, time out back to the flow
+  // (visually identical — on restore the overlay targets the track width).
+  useEffect(() => {
+    if (panelExpanded || !overlay) return
+    const timer = window.setTimeout(() => { setOverlay(false) }, 1000)
+    return () => { window.clearTimeout(timer) }
+  }, [overlay, panelExpanded])
+  const layout = cols
+  const colsRef = useRef(layout)
+  colsRef.current = layout
+  // The first grid track as a CSS expression: a shell may override the
+  // collapsed rail (--dsh-sidebar-collapsed-track; the desktop overlay uses
+  // 0), so only the expression — never the solved number — matches the
+  // rendered track. Shared by the grid template and the fullscreen target.
+  const sidebarTrack = sidebarCollapsed ? `var(--dsh-sidebar-collapsed-track, ${layout.sidebar}px)` : `${layout.sidebar}px`
+  // The panel interior's pinned width, as CSS width values. The fullscreen
+  // target subtracts the rendered sidebar track in the engine (a calc over
+  // the track expression), so the overlay hugs the sidebar exactly under
+  // track overrides. While the track collapses to zero the target reads 0px,
+  // but the pin must keep the last real width so the content slides out
+  // unchanged instead of re-wrapping to nothing. Under the overlay the pin
+  // rides the animated target, so the eased width only moves the clip box —
+  // the interior never re-wraps mid-flight.
+  //
+  // The column and the pin resolve their percentages against DIFFERENT
+  // boxes: the column's containing block is the frame (so `100%` is the
+  // frame width), but the pin sits inside the column, so its `100%` would
+  // read the column's own animated width and subtract the sidebar a second
+  // time — a blank strip the width of the sidebar at the window's right
+  // edge, re-wrapping mid-flight. The pin therefore uses `100cqi`: the
+  // frame's container-query inline size, a constant during the ease, which
+  // keeps the clip-box design exact (the eased column width only moves the
+  // clip; the interior never follows it).
+  const lastPanelWidth = useRef('0px')
+  const boxTarget = panelExpanded ? `calc(100% - ${sidebarTrack})` : `${layout.panel}px`
+  const pinBoxTarget = panelExpanded ? `calc(100cqi - ${sidebarTrack})` : `${layout.panel}px`
+  if (pinBoxTarget !== '0px') lastPanelWidth.current = pinBoxTarget
+  const pinTarget = overlay ? pinBoxTarget : `${layout.panel}px`
+  const pinWidth = pinTarget !== '0px' ? pinTarget : lastPanelWidth.current
 
   // The drag base is the rendered width captured at drag start (grabbing a
   // concession-clamped panel must not jump back to the stored preference);
   // it stays frozen for the whole gesture so dx deltas do not compound.
   const sidebarBase = useRef(0)
   const detailsBase = useRef(0)
+  const panelBase = useRef(0)
   // Track-level transitions pause for the whole gesture: eased tracks would
   // detach the column edge from the pointer (AppFrame.module.css).
   const [dragging, setDragging] = useState(false)
   const onDragEnd = useCallback(() => { setDragging(false) }, [])
   const onSidebarStart = useCallback(() => { sidebarBase.current = colsRef.current.sidebar; setDragging(true) }, [])
   const onDetailsStart = useCallback(() => { detailsBase.current = colsRef.current.details; setDragging(true) }, [])
+  const onPanelStart = useCallback(() => {
+    panelBase.current = colsRef.current.panel
+    setDragging(true)
+  }, [])
   const onSidebarDrag = useCallback((dx: number) => {
     actions.setSidebar(sidebarBase.current + dx)
   }, [actions])
   const onDetailsDrag = useCallback((dx: number) => {
     actions.setDetails(detailsBase.current - dx)
   }, [actions])
+  const onPanelDrag = useCallback((dx: number) => {
+    actions.setPanel(panelBase.current - dx)
+  }, [actions])
 
+  // Collapsed rail stays SIDEBAR_COLLAPSED unless a shell sets
+  // --dsh-sidebar-collapsed-track (desktop overlay uses 0).
   return (
     <div
       ref={frameRef}
       className={css.frame}
-      style={{ gridTemplateColumns: `${cols.sidebar}px minmax(0, 1fr) ${cols.details}px` }}
+      style={{ gridTemplateColumns: `${sidebarTrack} minmax(0, 1fr) ${layout.details}px ${layout.panel}px` }}
       data-sidebar-collapsed={sidebarCollapsed || undefined}
-      data-details-collapsed={cols.details === 0 || undefined}
+      data-details-collapsed={(layout.details === 0) || undefined}
+      data-panel-collapsed={(layout.panel === 0) || undefined}
+      data-panel-expanded={panelExpanded || undefined}
+      data-panel-overlay={overlay || undefined}
       data-dragging={dragging || undefined}
     >
       <div className={css.sidebarCol}>
@@ -175,10 +263,11 @@ export function AppFrame({
             sidebar keeps the mounted slot at the compact-rail width, and the
             component sees its rendered state as owner params decided here
             (collapsed follows the resolved rail, so a derived auto-collapse
-            renders the rail UI too). */}
+            renders the rail UI too). Panel fullscreen keeps the sidebar
+            track untouched. */}
         {renderSlot('sidebar', {
           collapsed: sidebarCollapsed,
-          width: cols.sidebar,
+          width: layout.sidebar,
         })}
       </div>
       <>
@@ -187,15 +276,45 @@ export function AppFrame({
             the shell's own pending rendering. The conversation
             is session-maybe; the strict details entry naturally renders
             empty while no session is current. */}
-        <CenterColumn>{renderSlot('conversation', {})}</CenterColumn>
-        <DetailsColumn>{renderSlot('details', {})}</DetailsColumn>
+        <CenterColumn><StaticSlot renderSlot={renderSlot} slotKey="conversation" /></CenterColumn>
+        <DetailsColumn><StaticSlot renderSlot={renderSlot} slotKey="details" /></DetailsColumn>
+        {/* Panel column grid item; width 0 keeps the subtree mounted (never
+            unmount on close). The inner layer pins to the column's LEFT
+            edge at the target width, so the eased track motion (open,
+            close) reveals the panel content without ever re-wrapping it.
+            Fullscreen swaps the presentation to a right-anchored overlay:
+            the inline width eases between the track width and the
+            content-area width, and only when the restore transition ends
+            does the column rejoin the flow — at the track width, so the
+            switch is seamless. The in-flow width is the pin width in px
+            (explicit px keeps the expand transition interpolable — auto
+            would not animate — while matching the track exactly). */}
+        <div
+          className={css.panelCol}
+          style={{ width: overlay ? boxTarget : pinWidth }}
+          onTransitionEnd={(event) => {
+            if (event.target === event.currentTarget && event.propertyName === 'width' && !panelExpanded) setOverlay(false)
+          }}
+        >
+          <div className={css.panelColPin} style={{ width: pinWidth }}>
+            <StaticSlot renderSlot={renderSlot} slotKey="panel" />
+          </div>
+        </div>
       </>
       <div className={css.overlayLayer} data-shell-overlay>
-        {renderSlot('shell.overlay', {})}
+        <StaticSlot renderSlot={renderSlot} slotKey="shell.overlay" />
       </div>
-      {/* The collapsed rail is fixed-width: no resize handle while closed. */}
-      {!sidebarCollapsed && <DragHandle side="sidebar" left={cols.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
-      {cols.details > 0 && <DragHandle side="details" left={viewport - cols.details} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
+      {/* The collapsed rail is fixed-width: no resize handle while closed.
+          The overlay covers the details column and its handle (frozen
+          underneath), keeps the sidebar handle (usable at the overlay's
+          left edge), and suspends the panel handle for the whole overlay
+          episode (expand, eased restore, release). The details handle sits
+          on the center|details seam, which yields to the open panel column:
+          the panel opens to the RIGHT of details, so the seam's left offset
+          subtracts both right-side tracks. */}
+      {!sidebarCollapsed && <DragHandle side="sidebar" left={layout.sidebar} onStart={onSidebarStart} onDrag={onSidebarDrag} onEnd={onDragEnd} />}
+      {!overlay && layout.details > 0 && <DragHandle side="details" left={viewport - layout.details - layout.panel} onStart={onDetailsStart} onDrag={onDetailsDrag} onEnd={onDragEnd} />}
+      {!overlay && layout.panel > 0 && <DragHandle side="panel" left={viewport - layout.panel} onStart={onPanelStart} onDrag={onPanelDrag} onEnd={onDragEnd} />}
     </div>
   )
 }
diff --git a/packages/client/ui-layout/src/client/columns.ts b/packages/client/ui-layout/src/client/columns.ts
index a038f6726b..6a2fedf12c 100644
--- a/packages/client/ui-layout/src/client/columns.ts
+++ b/packages/client/ui-layout/src/client/columns.ts
@@ -1,24 +1,25 @@
 /**
- * Pure concession-chain column solver for the three-column AppFrame.
- * Chain order is fixed by contract: keep center >= CENTER_MIN by shrinking
- * details, then auto-closing it (derived zero width — preferred width
- * preferences are never rewritten, so widening the window restores them).
- * The sidebar never concedes: its rendered width is always the drag
- * preference (or the collapsed rail), and center absorbs any remaining
- * deficit as the last resort. Inputs are the layout store's plain width
- * preferences (0 = closed); a closed sidebar resolves to the fixed
- * SIDEBAR_COLLAPSED control rail while closed details resolve to zero width.
- * The SIDEBAR_AUTO_COLLAPSE breakpoint is consumed by AppFrame, which decides
+ * Pure concession-chain column solver for the three-column-plus-panel
+ * AppFrame. Chain order is fixed by contract: keep center >= CENTER_MIN by
+ * shrinking details, then the panel, then auto-closing them in the same
+ * order (derived zero widths — preferred width preferences are never
+ * rewritten, so widening the window restores them). The sidebar never
+ * concedes: its rendered width is always the drag preference (or the
+ * collapsed rail), and center absorbs any remaining deficit as the last
+ * resort. Inputs are the layout store's plain width preferences (0 =
+ * closed); a closed sidebar resolves to the fixed SIDEBAR_COLLAPSED control
+ * rail while closed details and panel resolve to zero width. The
+ * SIDEBAR_AUTO_COLLAPSE breakpoint is consumed by AppFrame, which decides
  * the effective sidebar preference before solving; the solver itself stays
  * breakpoint-free.
  */
 
 /** Resolved widths for one frame; center may drop below CENTER_MIN only at the final fallback. */
-export interface Columns { sidebar: number; center: number; details: number }
+export interface Columns { sidebar: number; center: number; details: number; panel: number }
 
 // Contract-frozen geometry: the three-column concession chain's fixed points.
 /** Center column floor; only the final fallback may go below it. */
-export const CENTER_MIN = 640
+export const CENTER_MIN = 360
 /** Sidebar drag clamp floor. */
 export const SIDEBAR_MIN = 264
 /** Sidebar drag clamp ceiling. */
@@ -37,6 +38,16 @@ export const DETAILS_MIN = 300
 export const DETAILS_MAX = 520
 /** Details width before any user drag. */
 export const DETAILS_DEFAULT = 360
+/** Panel (side panel column) width floor. */
+export const PANEL_MIN = 320
+/** Panel width when opened (the contract width is the only open width until a drag rewrites it). */
+export const PANEL_DEFAULT = 480
+
+/** Normalize a finite panel preference; the viewport solver owns its dynamic ceiling. */
+export function normalizePanelWidth(px: number): number {
+  if (!Number.isFinite(px)) return PANEL_MIN
+  return Math.max(PANEL_MIN, Math.round(px))
+}
 
 /**
  * Clamp a panel width into its contract range.
@@ -50,28 +61,49 @@ export function clampWidth(px: number, min: number, max: number): number {
 }
 
 /**
- * Solve the three column widths for one viewport frame. Pure: no hysteresis —
+ * Solve the four column widths for one viewport frame. Pure: no hysteresis —
  * the output is a function of (viewport, preferences) only, so recovery on
  * re-widening is automatic. Preferences re-clamp here because they cross the
  * store boundary and callers may still supply stale ranges.
  * @param viewport - available frame width in px.
  * @param sidebar - sidebar width preference in px (0 = closed).
  * @param details - details width preference in px (0 = closed).
- * @returns resolved widths; details 0 means visually closed (never unmounted), while a closed sidebar keeps its compact rail.
+ * @param panel - panel width preference in px (0 = closed).
+ * @returns resolved widths; details 0 means visually closed (never unmounted), while a closed sidebar keeps its compact rail and a closed panel stays mounted at zero width.
  */
-export function computeColumns(viewport: number, sidebar: number, details: number): Columns {
+export function computeColumns(viewport: number, sidebar: number, details: number, panel: number): Columns {
   // The sidebar is fixed at its preference (or the rail) — it never concedes.
   const s = sidebar === 0 ? SIDEBAR_COLLAPSED : clampWidth(sidebar, SIDEBAR_MIN, SIDEBAR_MAX)
   const d0 = details === 0 ? 0 : clampWidth(details, DETAILS_MIN, DETAILS_MAX)
+  const p0 = panel === 0 ? 0 : normalizePanelWidth(panel)
 
   // Step 1: everything fits at preferred widths.
-  if (s + d0 + CENTER_MIN <= viewport) return { sidebar: s, center: viewport - s - d0, details: d0 }
+  if (s + d0 + p0 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: viewport - s - d0 - p0, details: d0, panel: p0 }
+  }
 
   // Step 2: shrink details toward its minimum.
-  const d1 = d0 === 0 ? 0 : Math.max(DETAILS_MIN, viewport - s - CENTER_MIN)
-  if (s + d1 + CENTER_MIN <= viewport) return { sidebar: s, center: CENTER_MIN, details: d1 }
+  const d1 = d0 === 0 ? 0 : Math.max(DETAILS_MIN, viewport - s - p0 - CENTER_MIN)
+  if (s + d1 + p0 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: CENTER_MIN, details: d1, panel: p0 }
+  }
+
+  // Step 3: shrink the panel toward its minimum (details at its shrunk width).
+  const p1 = p0 === 0 ? 0 : Math.max(PANEL_MIN, viewport - s - d1 - CENTER_MIN)
+  if (s + d1 + p1 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: CENTER_MIN, details: d1, panel: p1 }
+  }
+
+  // Step 4: auto-close details (derived — preferences untouched), then shrink
+  // the panel against the full remaining space. The shrink never exceeds the
+  // panel's own preference: closing details frees width, it does not widen
+  // the panel beyond what the user asked for.
+  const p2 = p0 === 0 ? 0 : Math.min(p0, Math.max(PANEL_MIN, viewport - s - CENTER_MIN))
+  if (s + p2 + CENTER_MIN <= viewport) {
+    return { sidebar: s, center: viewport - s - p2, details: 0, panel: p2 }
+  }
 
-  // Step 3: auto-close details (derived — preferences untouched); center
-  // absorbs any remaining deficit (may drop below CENTER_MIN).
-  return { sidebar: s, center: Math.max(0, viewport - s), details: 0 }
+  // Step 5: auto-close the panel too; center absorbs any remaining deficit
+  // (may drop below CENTER_MIN).
+  return { sidebar: s, center: Math.max(0, viewport - s), details: 0, panel: 0 }
 }
diff --git a/packages/client/ui-layout/src/client/index.ts b/packages/client/ui-layout/src/client/index.ts
index c56d83bfbb..bdc3cbd130 100644
--- a/packages/client/ui-layout/src/client/index.ts
+++ b/packages/client/ui-layout/src/client/index.ts
@@ -1,7 +1,7 @@
 /**
  * Layout plugin, browser half: one register() call contributes AppFrame into
  * the runtime's built-in 'root' slot and, in the same breath, declares the
- * four child slots (declaration = exclusive render authority), seats the
+ * five child slots (declaration = exclusive render authority), seats the
  * layout store (panel geometry), and wires the panel-action service face.
  * ctx.layout is the cross-plugin panel-action contract; navigation state lives
  * with the runtime sessions service. A second effect seats the theme
@@ -33,7 +33,7 @@ declare module '@deepseek-ai/cordis' {
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface SlotMap {
     // The 'root' entry itself is the runtime's built-in slot (declared
-    // there); these four are the frame's children, declared by the same
+    // there); these five are the frame's children, declared by the same
     // register() call that contributes AppFrame. Session owners never pass
     // sessionId: the framework injects it as a standard prop.
     /**
@@ -70,6 +70,18 @@ declare module '@deepseek-ai/dsh-client-ui-slots' {
      * `session` scope, and `ctx.layout` owns whether the column is open.
      */
     'details': { kind: 'single'; scope: 'session'; owner: DetailsOwnerProps }
+    /**
+     * The right side-panel column, shown when the layout opens it. OCCUPIED
+     * by a panel container (which declares its page seats inside it) —
+     * registering here replaces the column and takes those seats with it.
+     * Absent an occupant the column renders nothing.
+     *
+     * Current-session-optional like `conversation`: the panel survives
+     * session switches (its own chrome is session-independent), while the
+     * page seats inside are session-scoped. `ctx.layout` owns whether the
+     * column is open (toggle/open/close).
+     */
+    'panel': { kind: 'single'; scope: 'session-maybe'; owner: PanelOwnerProps }
     /**
      * Frame-wide floating layer, above every column and outside their scroll
      * containers. Deliberately generic and unowned by any feature: a badge, a
@@ -104,12 +116,15 @@ export interface ConvOwnerProps {}
 /** Details owner share: empty — sessionId arrives as a framework-standard prop. */
 export interface DetailsOwnerProps {}
 
+/** Panel owner share: empty — the container reads the layout share and session hooks itself. */
+export interface PanelOwnerProps {}
+
 /** Required services (cordis fiber inject — the loader passes all module exports as an object plugin). */
 export const inject = ['slots', 'theme']
 
 /**
  * Client plugin body: provide ctx.layout, then one register() call — AppFrame
- * into 'root' with the four child-slot declarations, the layout store seat,
+ * into 'root' with the five child-slot declarations, the layout store seat,
  * and the inject hook that hands the store's bound actions to the service.
  * @param ctx - client root context.
  */
@@ -123,6 +138,7 @@ export function apply(ctx: ClientContext): void {
         'sidebar': { kind: 'single', scope: 'root' },
         'conversation': { kind: 'single', scope: 'session-maybe' },
         'details': { kind: 'single', scope: 'session' },
+        'panel': { kind: 'single', scope: 'session-maybe' },
         'shell.overlay': { kind: 'list', scope: 'root' },
       },
       // Exclusive store: the factory itself — the framework instantiates per
diff --git a/packages/client/ui-layout/src/client/service.ts b/packages/client/ui-layout/src/client/service.ts
index 2b2e50e5d5..310c0fe44e 100644
--- a/packages/client/ui-layout/src/client/service.ts
+++ b/packages/client/ui-layout/src/client/service.ts
@@ -27,6 +27,17 @@ export interface ILayout {
   openDetails(): void
   /** Close the details panel. */
   closeDetails(): void
+  /** Toggle the side panel column (closed ⟷ contract default width). */
+  togglePanel(): void
+  /** Open the side panel column (no-op when already open). */
+  openPanel(): void
+  /** Close the side panel column. */
+  closePanel(): void
+  /**
+   * Toggle panel fullscreen over an open panel (no-op while closed); closing
+   * the panel resets the flag.
+   */
+  togglePanelExpanded(): void
 }
 
 /** Cross-plugin panel-action face (ctx.layout). */
@@ -59,6 +70,29 @@ export class LayoutController implements ILayout {
     this.#require().closeDetails()
   }
 
+  /** Toggle the side panel column (closed ⟷ contract default width). */
+  togglePanel(): void {
+    this.#require().togglePanel()
+  }
+
+  /** Open the side panel column (no-op when already open). */
+  openPanel(): void {
+    this.#require().openPanel()
+  }
+
+  /** Close the side panel column. */
+  closePanel(): void {
+    this.#require().closePanel()
+  }
+
+  /**
+   * Toggle panel fullscreen over an open panel (no-op while closed); closing
+   * the panel resets the flag.
+   */
+  togglePanelExpanded(): void {
+    this.#require().togglePanelExpanded()
+  }
+
   #require(): PanelActions {
     // Callers are UI gestures, which cannot fire before the root entry
     // rendered (the inject hook runs in its first render) — reaching this
diff --git a/packages/client/ui-layout/src/client/stores.ts b/packages/client/ui-layout/src/client/stores.ts
index d2de668a9c..13ed15bdd8 100644
--- a/packages/client/ui-layout/src/client/stores.ts
+++ b/packages/client/ui-layout/src/client/stores.ts
@@ -10,17 +10,27 @@
 import { defineStore, type EngineStoreHandle } from '@deepseek-ai/dsh-client-runtime/client'
 import {
   clampWidth, DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
+  normalizePanelWidth, PANEL_DEFAULT,
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from './columns.ts'
 
 /**
- * Layout store state: panel width preferences in px (0 = closed), plus the
- * narrow-viewport pair — `narrow` mirrors AppFrame's breakpoint reading
+ * Layout store state: panel width preferences in px (0 = closed), the
+ * panel-fullscreen flag (`panelExpanded` — the open panel takes over the
+ * content area; only meaningful while `panel > 0`), plus the narrow-viewport
+ * pair — `narrow` mirrors AppFrame's breakpoint reading
  * (viewport < SIDEBAR_AUTO_COLLAPSE) so toggleSidebar can pick semantics, and
  * `narrowExpanded` is the manual override that re-expands the auto-collapsed
  * sidebar over the squeezed center without rewriting the width preference.
  */
-type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowExpanded: boolean }
+type LayoutState = {
+  sidebar: number
+  details: number
+  panel: number
+  panelExpanded: boolean
+  narrow: boolean
+  narrowExpanded: boolean
+}
 
 /**
  * Annotation twin of the actions literal below (the export needs a declared
@@ -29,10 +39,15 @@ type LayoutState = { sidebar: number; details: number; narrow: boolean; narrowEx
 type LayoutActions = {
   setSidebar: (draft: LayoutState, px: number) => void
   setDetails: (draft: LayoutState, px: number) => void
+  setPanel: (draft: LayoutState, px: number) => void
   toggleSidebar: (draft: LayoutState) => void
   setNarrow: (draft: LayoutState, narrow: boolean) => void
   openDetails: (draft: LayoutState) => void
   closeDetails: (draft: LayoutState) => void
+  togglePanel: (draft: LayoutState) => void
+  openPanel: (draft: LayoutState) => void
+  closePanel: (draft: LayoutState) => void
+  togglePanelExpanded: (draft: LayoutState) => void
 }
 
 /**
@@ -47,10 +62,13 @@ type LayoutActions = {
  */
 export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutActions>  {
   const handle = defineStore({
-    init: (): LayoutState => ({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false }),
+    init: (): LayoutState => ({
+      sidebar: SIDEBAR_DEFAULT, details: 0, panel: 0, panelExpanded: false, narrow: false, narrowExpanded: false,
+    }),
     actions: {
       setSidebar: (d, px: number) => { d.sidebar = clampWidth(px, SIDEBAR_MIN, SIDEBAR_MAX) },
       setDetails: (d, px: number) => { d.details = clampWidth(px, DETAILS_MIN, DETAILS_MAX) },
+      setPanel: (d, px: number) => { if (Number.isFinite(px)) d.panel = normalizePanelWidth(px) },
       // Narrow toggles flip only the override: the width preference survives
       // untouched, so re-widening restores the pre-squeeze layout.
       toggleSidebar: (d) => {
@@ -66,6 +84,19 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
       },
       openDetails: (d) => { if (d.details === 0) d.details = DETAILS_DEFAULT },
       closeDetails: (d) => { d.details = 0 },
+      // Toggle is the open/closed write set: closing forgets the drag width
+      // and reopening restores the contract default (clamp keeps the range
+      // declared in one place for the solver). Closing exits fullscreen —
+      // the flag is only meaningful over an open panel.
+      togglePanel: (d) => {
+        if (d.panel === 0) d.panel = PANEL_DEFAULT
+        else { d.panel = 0; d.panelExpanded = false }
+      },
+      openPanel: (d) => { d.panel = normalizePanelWidth(d.panel === 0 ? PANEL_DEFAULT : d.panel) },
+      closePanel: (d) => { d.panel = 0; d.panelExpanded = false },
+      // Fullscreen flips only over an open panel; AppFrame additionally
+      // treats the flag as false while the solved width is zero.
+      togglePanelExpanded: (d) => { if (d.panel > 0) d.panelExpanded = !d.panelExpanded },
     },
   })
   return handle
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index d865a5c186..cf69a7930e 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -5,8 +5,8 @@
  * path), a recording renderSlot stub, and a render-prop SessionProvider stub
  * (the real one is framework-wired to the renderer host; its own behavior is
  * ui-renderer's spec territory). Drag sequences (pointer capture + rAF flush),
- * concession response to viewport change, and details staying mounted at
- * zero width are the preserved behavior assertions. jsdom has no layout
+ * concession response to viewport change, and details/panel staying mounted
+ * at zero width are the preserved behavior assertions. jsdom has no layout
  * engine, so the frame width comes from a mocked getBoundingClientRect and
  * resizes are driven through the ResizeObserver stub.
  */
@@ -61,6 +61,7 @@ function mountFrame() {
     if (key === 'sidebar') return <div data-testid="sidebar-content" />
     if (key === 'conversation') return <div data-testid="center-content" />
     if (key === 'details') return <div data-testid="details-content" />
+    if (key === 'panel') return <div data-testid="panel-content" />
     if (key === 'conversation.empty') return <div data-testid="empty-content" />
     return <div data-testid="other-content" />
   }) as AppFrameProps['renderSlot']
@@ -95,10 +96,11 @@ function mountFrame() {
   return { instance, frame, slotCalls, rerenderFrame: () => { utils.rerender(element()) }, ...utils }
 }
 
+/** [sidebar, details, panel] rendered widths from the live template string. */
 function tracks(frame: HTMLElement): number[] {
-  const m = /^(\d+)px minmax\(0, 1fr\) (\d+)px$/.exec(frame.style.gridTemplateColumns)
+  const m = /^(?:var\(--dsh-sidebar-collapsed-track, )?(\d+)px(?:\))? minmax\(0, 1fr\) (\d+)px (\d+)px$/.exec(frame.style.gridTemplateColumns)
   if (m === null) throw new Error(`unexpected template: ${frame.style.gridTemplateColumns}`)
-  return [Number(m[1]), Number(m[2])]
+  return [Number(m[1]), Number(m[2]), Number(m[3])]
 }
 
 function drag(handle: Element, fromX: number, toX: number): void {
@@ -137,9 +139,9 @@ afterEach(() => {
 })
 
 describe('AppFrame', () => {
-  it('renders three tracks from store state', () => {
+  it('renders four tracks from store state', () => {
     const { frame } = mountFrame()
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
   })
 
   it('renders the session pair with empty owner shares (sessionId is framework-standard)', () => {
@@ -149,9 +151,11 @@ describe('AppFrame', () => {
     const keys = slotCalls.map(c => c.key)
     expect(keys).toContain('conversation')
     expect(keys).toContain('details')
+    expect(keys).toContain('panel')
     expect(keys).not.toContain('conversation.empty')
     expect(slotCalls.find(c => c.key === 'conversation')!.props).toEqual({})
     expect(slotCalls.find(c => c.key === 'details')!.props).toEqual({})
+    expect(slotCalls.find(c => c.key === 'panel')!.props).toEqual({})
   })
 
   it('keeps the conversation slot mounted while no session is current', () => {
@@ -174,59 +178,73 @@ describe('AppFrame', () => {
 
   it('ignores unselected states and closes only when the Session id changes', () => {
     const { frame, instance, rerenderFrame } = mountFrame()
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
 
     act(() => { instance.actions.openDetails() })
-    expect(tracks(frame)).toEqual([280, 360])
+    expect(tracks(frame)).toEqual([320, 360, 0])
 
     selectedSession.current = 's-next' as SessionId
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
 
     act(() => { instance.actions.openDetails() })
     selectedSession.current = 's-blank' as SessionId
     selectedSessionBlank.current = true
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     expect(instance.getSnapshot().details).toBe(360)
 
     selectedSession.current = 's-next' as SessionId
     selectedSessionBlank.current = false
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 360])
+    expect(tracks(frame)).toEqual([320, 360, 0])
 
     selectedSession.current = undefined
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     selectedSession.current = 's-test' as SessionId
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
   })
 
   it('keeps details closed when the first Session materializes', () => {
     selectedSession.current = undefined
     const { frame, instance, rerenderFrame } = mountFrame()
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     expect(instance.getSnapshot().details).toBe(0)
 
     selectedSession.current = 's-first' as SessionId
     act(() => { rerenderFrame() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
   })
 
   it('sidebar slot receives live concession output as owner props', () => {
     const { slotCalls } = mountFrame()
-    expect(slotCalls.find(c => c.key === 'sidebar')!.props).toEqual({ collapsed: false, width: 280 })
+    expect(slotCalls.find(c => c.key === 'sidebar')!.props).toEqual({ collapsed: false, width: 320 })
+  })
+
+  it('does not redispatch static slot subtrees on layout-only store ticks', () => {
+    const { instance, slotCalls } = mountFrame()
+    const staticKeys = ['conversation', 'details', 'panel', 'shell.overlay']
+    const before = new Map(staticKeys.map(key => [key, slotCalls.filter(call => call.key === key).length]))
+
+    act(() => { instance.actions.openPanel() })
+    act(() => { instance.actions.setPanel(520) })
+
+    for (const key of staticKeys) {
+      expect(slotCalls.filter(call => call.key === key)).toHaveLength(before.get(key) ?? 0)
+    }
+    expect(slotCalls.filter(call => call.key === 'sidebar').length).toBeGreaterThan(1)
   })
 
   it('sidebar drag widens through rAF-batched pointer moves', () => {
     const { frame } = mountFrame()
     const handles = frame.querySelectorAll('[class*="handle"]')
-    drag(handles[0]!, 280, 350)
-    expect(tracks(frame)[0]).toBe(350)
+    drag(handles[0]!, 320, 390)
+    expect(tracks(frame)[0]).toBe(390)
   })
 
-  it('details drag widens leftward (negative dx grows the panel)', () => {
+  it('details drag widens leftward (negative dx grows the column)', () => {
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.openDetails() })
     const handles = frame.querySelectorAll('[class*="handle"]')
@@ -234,19 +252,41 @@ describe('AppFrame', () => {
     expect(tracks(frame)[1]).toBe(420)
   })
 
+  it('panel drag widens leftward through its own handle', () => {
+    const { frame, instance } = mountFrame()
+    act(() => { instance.actions.togglePanel() })
+    const handles = frame.querySelectorAll('[class*="handle"]')
+    expect(handles).toHaveLength(2)
+    drag(handles[1]!, 1440, 1380)
+    expect(tracks(frame)[2]).toBe(540)
+  })
+
+  it('details handle yields to the open panel column', () => {
+    const { frame, instance } = mountFrame()
+    act(() => { instance.actions.openDetails() })
+    act(() => { instance.actions.togglePanel() })
+    const handles = frame.querySelectorAll<HTMLElement>('[class*="handle"]')
+    expect(handles).toHaveLength(3)
+    // 1920 frame: sidebar 320 | center | details 360 | panel 480. The panel
+    // opens to the RIGHT of details, so the center|details seam sits at
+    // viewport - details - panel; the panel seam stays at viewport - panel.
+    expect(handles[1]!.style.left).toBe('1080px')
+    expect(handles[2]!.style.left).toBe('1440px')
+  })
+
   it('drag base is the rendered (concession-clamped) width, not the preference', () => {
-    frameWidth = 1250 // step-2 squeeze: details renders 330 while preference is 360
+    frameWidth = 1030 // step-2 squeeze: details renders 350 while preference is 360
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.openDetails() })
-    expect(tracks(frame)).toEqual([280, 330])
+    expect(tracks(frame)).toEqual([320, 350, 0])
     const handles = frame.querySelectorAll('[class*="handle"]')
-    drag(handles[1]!, 920, 930) // shrink by 10 from the rendered width
-    expect(instance.getSnapshot().details).toBe(320)
+    drag(handles[1]!, 680, 690) // shrink by 10 from the rendered width
+    expect(instance.getSnapshot().details).toBe(340)
   })
 
   it('details column stays mounted at zero width', () => {
     const { frame, getByTestId } = mountFrame()
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     expect(getByTestId('details-content')).toBeTruthy()
     expect(frame.hasAttribute('data-details-collapsed')).toBe(true)
   })
@@ -254,22 +294,85 @@ describe('AppFrame', () => {
   it('closed sidebar keeps its compact rail with mounted slot content and collapsed owner props', () => {
     const { frame, instance, slotCalls, getByTestId } = mountFrame()
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0, 0])
     expect(getByTestId('sidebar-content')).toBeTruthy()
     expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(true)
     const lastSidebarCall = slotCalls.filter(c => c.key === 'sidebar').at(-1)!
     expect(lastSidebarCall.props).toEqual({ collapsed: true, width: SIDEBAR_COLLAPSED })
   })
 
+  it('togglePanel opens the panel at the contract default with mounted content', () => {
+    const { frame, instance, getByTestId } = mountFrame()
+    expect(frame.hasAttribute('data-panel-collapsed')).toBe(true)
+    act(() => { instance.actions.togglePanel() })
+    expect(tracks(frame)).toEqual([320, 0, 480])
+    expect(frame.hasAttribute('data-panel-collapsed')).toBe(false)
+    expect(getByTestId('panel-content')).toBeTruthy()
+    act(() => { instance.actions.togglePanel() })
+    expect(tracks(frame)).toEqual([320, 0, 0])
+    // The subtree stays mounted at zero width (never unmount on close).
+    expect(getByTestId('panel-content')).toBeTruthy()
+    expect(frame.hasAttribute('data-panel-collapsed')).toBe(true)
+  })
+
+  it('panel fullscreen is an overlay: tracks untouched, eased exact restore', () => {
+    const { frame, instance } = mountFrame()
+    act(() => { instance.actions.togglePanelExpanded() })
+    // No-op while the panel is closed.
+    expect(frame.hasAttribute('data-panel-expanded')).toBe(false)
+    act(() => { instance.actions.togglePanel() })
+    act(() => { instance.actions.togglePanelExpanded() })
+    expect(frame.hasAttribute('data-panel-expanded')).toBe(true)
+    // Overlay presentation: the grid tracks are untouched (the center gets
+    // covered, never re-wrapped) and the column leaves the flow at the
+    // content-area width.
+    expect(tracks(frame)).toEqual([320, 0, 480])
+    // The presentation flag rides the frame root (same anchor family as the
+    // collapsed/expanded markers); the column is found via its CSS-module
+    // class — the established [class*=…] anchor for hashed names.
+    expect(frame.hasAttribute('data-panel-overlay')).toBe(true)
+    const col = frame.querySelector<HTMLElement>('[class*="panelCol"]')
+    expect(col).toBeTruthy()
+    expect(col!.style.width).toBe('calc(100% - 320px)')
+    // The interior pin resolves against the FRAME, not the column: its
+    // containing block is panelCol, so a `100%` here would subtract the
+    // sidebar twice (blank strip at the right edge) and re-wrap the content
+    // on every eased frame. `100cqi` keeps the clip-box design exact.
+    const pin = frame.querySelector<HTMLElement>('[class*="panelColPin"]')
+    expect(pin).toBeTruthy()
+    // (cssstyle reorders calc terms; assert the semantics, not the spelling.)
+    expect(pin!.style.width).toContain('100cqi')
+    expect(pin!.style.width).not.toContain('%')
+    // The overlay covers the details handle and the panel handle; the
+    // sidebar handle survives at the overlay's left edge.
+    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
+    act(() => { instance.actions.togglePanelExpanded() })
+    // The presentation outlives the intent so the width can ease back down.
+    expect(frame.hasAttribute('data-panel-expanded')).toBe(false)
+    expect(col!.style.width).toBe('480px')
+    // Mid-restore the pin is already at the track width: the interior rides
+    // the column's left edge (translation), never its animated width.
+    expect(pin!.style.width).toBe('480px')
+    const end = new Event('transitionend', { bubbles: true })
+    Object.assign(end, { propertyName: 'width' })
+    act(() => { col!.dispatchEvent(end) })
+    // Released back into the flow at the track width — a seamless switch.
+    expect(frame.hasAttribute('data-panel-overlay')).toBe(false)
+    expect(tracks(frame)).toEqual([320, 0, 480])
+    // Closing the panel resets the flag in the store.
+    act(() => { instance.actions.closePanel() })
+    expect(instance.getSnapshot().panelExpanded).toBe(false)
+  })
+
   it('viewport shrink triggers the concession chain via ResizeObserver', () => {
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.openDetails() })
-    frameWidth = 1250
+    frameWidth = 1030
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([280, 330])
+    expect(tracks(frame)).toEqual([320, 350, 0])
     frameWidth = 1920
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([280, 360])
+    expect(tracks(frame)).toEqual([320, 360, 0])
   })
 
   it('drag handles disappear for collapsed columns', () => {
@@ -279,6 +382,10 @@ describe('AppFrame', () => {
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(2)
     act(() => { instance.actions.closeDetails() })
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
+    act(() => { instance.actions.togglePanel() })
+    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(2)
+    act(() => { instance.actions.closePanel() })
+    expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
     act(() => { instance.actions.toggleSidebar() })
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(0)
   })
@@ -288,7 +395,7 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
   it('mounts collapsed below the breakpoint with no sidebar handle', () => {
     frameWidth = 980
     const { frame, slotCalls } = mountFrame()
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0, 0])
     expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(true)
     expect(slotCalls.filter(c => c.key === 'sidebar').at(-1)!.props).toEqual({ collapsed: true, width: SIDEBAR_COLLAPSED })
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(0)
@@ -298,11 +405,11 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
     frameWidth = 980
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     expect(frame.hasAttribute('data-sidebar-collapsed')).toBe(false)
     expect(frame.querySelectorAll('[class*="handle"]')).toHaveLength(1)
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0, 0])
   })
 
   it('a wide-closed preference re-expands at the contract default while narrow', () => {
@@ -312,7 +419,7 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
     frameWidth = 980
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
     expect(instance.getSnapshot().sidebar).toBe(0) // preference untouched
   })
 
@@ -321,10 +428,10 @@ describe('AppFrame — narrow-viewport auto-collapse', () => {
     act(() => { instance.actions.setSidebar(400) })
     frameWidth = 980
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0])
+    expect(tracks(frame)).toEqual([SIDEBAR_COLLAPSED, 0, 0])
     frameWidth = 1920
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([400, 0])
+    expect(tracks(frame)).toEqual([400, 0, 0])
   })
 })
 
@@ -345,28 +452,28 @@ describe('AppFrame — guard branches', () => {
   it('two moves inside one frame coalesce through the pending rAF', () => {
     const { frame, instance } = mountFrame()
     const handle = frame.querySelectorAll('[class*="handle"]')[0]!
-    act(() => { handle.dispatchEvent(new PointerEvent('pointerdown', { pointerId: 1, clientX: 280, bubbles: true })) })
+    act(() => { handle.dispatchEvent(new PointerEvent('pointerdown', { pointerId: 1, clientX: 320, bubbles: true })) })
     act(() => {
       // Two moves before the frame flushes: the second must ride the pending
       // rAF (frame.current ??= guard), and the flush sees the latest x.
-      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 320, bubbles: true }))
-      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 340, bubbles: true }))
+      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 360, bubbles: true }))
+      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 380, bubbles: true }))
       vi.advanceTimersByTime(20)
     })
-    act(() => { handle.dispatchEvent(new PointerEvent('pointerup', { pointerId: 1, clientX: 340, bubbles: true })) })
-    expect(instance.getSnapshot().sidebar).toBe(340)
+    act(() => { handle.dispatchEvent(new PointerEvent('pointerup', { pointerId: 1, clientX: 380, bubbles: true })) })
+    expect(instance.getSnapshot().sidebar).toBe(380)
   })
 
   it('pointerup with a pending rAF cancels it and commits the final position', () => {
     const { frame, instance } = mountFrame()
     const handle = frame.querySelectorAll('[class*="handle"]')[0]!
-    act(() => { handle.dispatchEvent(new PointerEvent('pointerdown', { pointerId: 1, clientX: 280, bubbles: true })) })
+    act(() => { handle.dispatchEvent(new PointerEvent('pointerdown', { pointerId: 1, clientX: 320, bubbles: true })) })
     act(() => {
-      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 360, bubbles: true }))
+      handle.dispatchEvent(new PointerEvent('pointermove', { pointerId: 1, clientX: 400, bubbles: true }))
       // No timer advance: the rAF is still pending when pointerup arrives.
-      handle.dispatchEvent(new PointerEvent('pointerup', { pointerId: 1, clientX: 360, bubbles: true }))
+      handle.dispatchEvent(new PointerEvent('pointerup', { pointerId: 1, clientX: 400, bubbles: true }))
     })
-    expect(instance.getSnapshot().sidebar).toBe(360)
+    expect(instance.getSnapshot().sidebar).toBe(400)
   })
 
   it('zero-width resize reports are ignored (display:none window)', () => {
@@ -374,7 +481,7 @@ describe('AppFrame — guard branches', () => {
     frameWidth = 0
     act(() => { fireResize?.(); vi.advanceTimersByTime(20) })
     // Track template still reflects the last non-zero viewport.
-    expect(tracks(frame)).toEqual([280, 0])
+    expect(tracks(frame)).toEqual([320, 0, 0])
   })
 })
 
@@ -391,8 +498,8 @@ describe('AppFrame — unmount with an in-flight resize frame', () => {
   it('double resize inside one frame rides the pending rAF (??= guard)', () => {
     const { frame, instance } = mountFrame()
     act(() => { instance.actions.openDetails() })
-    frameWidth = 1250
+    frameWidth = 1030
     act(() => { fireResize?.(); fireResize?.(); vi.advanceTimersByTime(20) })
-    expect(tracks(frame)).toEqual([280, 330])
+    expect(tracks(frame)).toEqual([320, 350, 0])
   })
 })
diff --git a/packages/client/ui-layout/tests/columns.client.spec.ts b/packages/client/ui-layout/tests/columns.client.spec.ts
index ae8c39a117..131d85d84d 100644
--- a/packages/client/ui-layout/tests/columns.client.spec.ts
+++ b/packages/client/ui-layout/tests/columns.client.spec.ts
@@ -1,7 +1,8 @@
 import { describe, expect, it } from 'vitest'
 import {
-  CENTER_MIN, clampWidth, computeColumns,
-  DETAILS_DEFAULT, DETAILS_MIN, SIDEBAR_COLLAPSED, SIDEBAR_DEFAULT, SIDEBAR_MIN,
+  CENTER_MIN, clampWidth, computeColumns, normalizePanelWidth,
+  DETAILS_DEFAULT, DETAILS_MIN, PANEL_DEFAULT, PANEL_MIN,
+  SIDEBAR_COLLAPSED, SIDEBAR_DEFAULT, SIDEBAR_MIN,
 } from '@deepseek-ai/dsh-client-ui-layout/src/client/columns.ts'
 
 // Numeric preference form (0 = closed); helpers keep the scenario names readable.
@@ -16,80 +17,123 @@ describe('clampWidth', () => {
   })
 })
 
+describe('normalizePanelWidth', () => {
+  it('keeps finite preferences without a fixed ceiling and rejects non-finite values', () => {
+    expect(normalizePanelWidth(1)).toBe(PANEL_MIN)
+    expect(normalizePanelWidth(1200.4)).toBe(1200)
+    expect(normalizePanelWidth(Number.NaN)).toBe(PANEL_MIN)
+    expect(normalizePanelWidth(Number.POSITIVE_INFINITY)).toBe(PANEL_MIN)
+  })
+})
+
 describe('computeColumns', () => {
   it('step 1: everything fits at preferred widths', () => {
-    const cols = computeColumns(1920, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
-    expect(cols).toEqual({ sidebar: 280, center: 1920 - 280 - 360, details: 360 })
+    const cols = computeColumns(1920, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
+    expect(cols).toEqual({
+      sidebar: 320, center: 1920 - 320 - 360 - PANEL_DEFAULT, details: 360, panel: PANEL_DEFAULT,
+    })
   })
 
-  it('closed sidebar keeps its compact rail while closed details contribute zero width', () => {
-    expect(computeColumns(1920, closed(300), closed(360)))
-      .toEqual({ sidebar: SIDEBAR_COLLAPSED, center: 1920 - SIDEBAR_COLLAPSED, details: 0 })
+  it('closed sidebar keeps its compact rail while closed details and panel stay mounted at zero', () => {
+    expect(computeColumns(1920, closed(300), closed(360), closed(PANEL_DEFAULT)))
+      .toEqual({ sidebar: SIDEBAR_COLLAPSED, center: 1920 - SIDEBAR_COLLAPSED, details: 0, panel: 0 })
   })
 
-  it('preferences beyond the clamp range are clamped before solving', () => {
-    const cols = computeColumns(1920, open(9999), open(1))
+  it('fixed-width preferences clamp while a panel preference has no fixed ceiling', () => {
+    const cols = computeColumns(20_000, open(9999), open(1), open(9999))
     expect(cols.sidebar).toBe(420)
     expect(cols.details).toBe(300)
-    expect(computeColumns(1920, open(1), open(DETAILS_DEFAULT)).sidebar).toBe(SIDEBAR_MIN)
+    expect(cols.panel).toBe(9999)
+    expect(computeColumns(1920, open(1), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT)).sidebar).toBe(SIDEBAR_MIN)
+  })
+
+  it('uses the viewport remainder as the panel\'s dynamic ceiling', () => {
+    expect(computeColumns(1512, open(SIDEBAR_DEFAULT), closed(DETAILS_DEFAULT), open(5000)))
+      .toEqual({ sidebar: 320, center: CENTER_MIN, details: 0, panel: 832 })
   })
 
-  it('step 2: details shrinks first, center pinned at min', () => {
-    // 280 + 360 + 640 = 1280 > 1250; details concedes to 1250-280-640 = 330.
-    const cols = computeColumns(1250, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
-    expect(cols).toEqual({ sidebar: 280, center: CENTER_MIN, details: 330 })
+  it('step 2: details shrinks first, center pinned at min, panel still at its preference', () => {
+    // 320 + 360 + 360 = 1040 > 1000; details concedes to 1000-320-360 = 320.
+    const cols = computeColumns(1000, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: 320, center: CENTER_MIN, details: 320, panel: 0 })
   })
 
   it('boundary: exactly at the step-1/step-2 seam', () => {
-    const cols = computeColumns(300 + 360 + CENTER_MIN, open(300), open(360))
-    expect(cols).toEqual({ sidebar: 300, center: CENTER_MIN, details: 360 })
-    const one = computeColumns(300 + 360 + CENTER_MIN - 1, open(300), open(360))
-    expect(one).toEqual({ sidebar: 300, center: CENTER_MIN, details: 359 })
+    const cols = computeColumns(300 + 360 + CENTER_MIN, open(300), open(360), closed(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: 300, center: CENTER_MIN, details: 360, panel: 0 })
+    const one = computeColumns(300 + 360 + CENTER_MIN - 1, open(300), open(360), closed(PANEL_DEFAULT))
+    expect(one).toEqual({ sidebar: 300, center: CENTER_MIN, details: 359, panel: 0 })
+  })
+
+  it('step 3: the panel shrinks after details (details at its shrunk width)', () => {
+    // 320 + 360 + 480 + 360 = 1520 > 1380; step 2: details → 300, still
+    // starved (320+300+480+360 = 1460 > 1380); step 3: panel → 400.
+    const cols = computeColumns(1380, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: 320, center: CENTER_MIN, details: 300, panel: 400 })
+  })
+
+  it('step 4: details auto-closes when its min still starves center — sidebar holds its preference', () => {
+    // 320 + 300 + 360 = 980 > 970 → details 0; center takes the free remainder.
+    const cols = computeColumns(970, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: 320, center: 650, details: 0, panel: 0 })
+  })
+
+  it('step 4 with an open panel: details closes, the panel shrinks against the freed space', () => {
+    // At 1200, details-min + panel-min still starves center. Closing details
+    // frees enough room to preserve the 480 panel preference.
+    const cols = computeColumns(1200, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: 320, center: 400, details: 0, panel: PANEL_DEFAULT })
   })
 
-  it('step 3: details auto-closes when its min still starves center — sidebar holds its preference', () => {
-    // 280 + 300 + 640 = 1220 > 1210 → details 0; sidebar untouched: center = 1210-280 = 930.
-    const cols = computeColumns(1210, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
-    expect(cols).toEqual({ sidebar: 280, center: 930, details: 0 })
+  it('step 5: the panel auto-closes when its floor still starves center', () => {
+    // 320 + 320 + 360 = 1000 > 990 → panel 0 too; center absorbs the remainder.
+    const cols = computeColumns(990, open(SIDEBAR_DEFAULT), closed(DETAILS_DEFAULT), open(PANEL_MIN))
+    expect(cols).toEqual({ sidebar: 320, center: 670, details: 0, panel: 0 })
   })
 
   it('the sidebar never concedes: center absorbs the deficit below CENTER_MIN', () => {
-    // 700 < 280+640: sidebar keeps 280, center takes 420 < CENTER_MIN.
-    const cols = computeColumns(700, open(SIDEBAR_DEFAULT), closed(DETAILS_DEFAULT))
-    expect(cols).toEqual({ sidebar: SIDEBAR_DEFAULT, center: 420, details: 0 })
+    // 650 < 320+360: sidebar keeps 320, center takes 330 < CENTER_MIN.
+    const cols = computeColumns(650, open(SIDEBAR_DEFAULT), closed(DETAILS_DEFAULT), closed(PANEL_DEFAULT))
+    expect(cols).toEqual({ sidebar: SIDEBAR_DEFAULT, center: 330, details: 0, panel: 0 })
   })
 
   it('sidebar-closed narrow window: details concedes then auto-closes', () => {
-    const fits = computeColumns(SIDEBAR_COLLAPSED + DETAILS_MIN + CENTER_MIN, closed(300), open(DETAILS_DEFAULT))
-    expect(fits).toEqual({ sidebar: SIDEBAR_COLLAPSED, center: CENTER_MIN, details: DETAILS_MIN })
-    const starved = computeColumns(SIDEBAR_COLLAPSED + DETAILS_MIN + CENTER_MIN - 1, closed(300), open(DETAILS_DEFAULT))
+    const fits = computeColumns(SIDEBAR_COLLAPSED + DETAILS_MIN + CENTER_MIN, closed(300), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT))
+    expect(fits).toEqual({ sidebar: SIDEBAR_COLLAPSED, center: CENTER_MIN, details: DETAILS_MIN, panel: 0 })
+    const starved = computeColumns(
+      SIDEBAR_COLLAPSED + DETAILS_MIN + CENTER_MIN - 1, closed(300), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT),
+    )
     expect(starved).toEqual({
       sidebar: SIDEBAR_COLLAPSED,
       center: DETAILS_MIN + CENTER_MIN - 1,
       details: 0,
+      panel: 0,
     })
   })
 
-  it('tiny viewport: details closes, sidebar holds, center takes the remainder', () => {
-    const cols = computeColumns(400, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
+  it('tiny viewport: details and panel close, sidebar holds, center takes the remainder', () => {
+    const cols = computeColumns(400, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
     expect(cols.details).toBe(0)
+    expect(cols.panel).toBe(0)
     expect(cols.sidebar).toBe(SIDEBAR_DEFAULT)
     expect(cols.center).toBe(Math.max(0, 400 - SIDEBAR_DEFAULT))
   })
 
   it('recovery is pure: re-widening restores preferred widths untouched', () => {
-    const squeezed = computeColumns(1100, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
+    const squeezed = computeColumns(900, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
     expect(squeezed.details).toBe(0)
-    const restored = computeColumns(1920, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT))
+    expect(squeezed.panel).toBe(0)
+    const restored = computeColumns(1920, open(SIDEBAR_DEFAULT), open(DETAILS_DEFAULT), open(PANEL_DEFAULT))
     expect(restored.details).toBe(DETAILS_DEFAULT)
+    expect(restored.panel).toBe(PANEL_DEFAULT)
     expect(restored.sidebar).toBe(SIDEBAR_DEFAULT)
   })
 })
 
 describe('computeColumns — degenerate viewports', () => {
   it('sidebar closed and viewport below CENTER_MIN: details auto-closes, center takes the rest', () => {
-    // Reaches step 3's auto-close with the compact rail sidebar.
-    expect(computeColumns(500, closed(300), open(DETAILS_DEFAULT)))
-      .toEqual({ sidebar: SIDEBAR_COLLAPSED, center: 500 - SIDEBAR_COLLAPSED, details: 0 })
+    // Reaches the auto-close chain with the compact rail sidebar.
+    expect(computeColumns(500, closed(300), open(DETAILS_DEFAULT), closed(PANEL_DEFAULT)))
+      .toEqual({ sidebar: SIDEBAR_COLLAPSED, center: 500 - SIDEBAR_COLLAPSED, details: 0, panel: 0 })
   })
 })
diff --git a/packages/client/ui-layout/tests/layout-store.client.spec.ts b/packages/client/ui-layout/tests/layout-store.client.spec.ts
index c6f0069197..61cc3f6fc0 100644
--- a/packages/client/ui-layout/tests/layout-store.client.spec.ts
+++ b/packages/client/ui-layout/tests/layout-store.client.spec.ts
@@ -9,6 +9,7 @@ import { beforeEach, describe, expect, it } from 'vitest'
 import { createLayoutStore } from '@deepseek-ai/dsh-client-ui-layout/src/client/stores.ts'
 import {
   DETAILS_DEFAULT, DETAILS_MAX, DETAILS_MIN,
+  PANEL_DEFAULT, PANEL_MIN,
   SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from '@deepseek-ai/dsh-client-ui-layout/src/client/columns.ts'
 
@@ -17,9 +18,11 @@ const PERSIST_KEY = 'dsh.layout.panels'
 beforeEach(() => { localStorage.clear() })
 
 describe('createLayoutStore', () => {
-  it('initializes the sidebar at its default width, details closed, wide viewport assumed', () => {
+  it('initializes the sidebar at its default width, details and panel closed, wide viewport assumed', () => {
     const { store } = createLayoutStore().create()
-    expect(store.getSnapshot()).toEqual({ sidebar: SIDEBAR_DEFAULT, details: 0, narrow: false, narrowExpanded: false })
+    expect(store.getSnapshot()).toEqual({
+      sidebar: SIDEBAR_DEFAULT, details: 0, panel: 0, panelExpanded: false, narrow: false, narrowExpanded: false,
+    })
   })
 
   it('each create() is an independent instance (factory is not a singleton)', () => {
@@ -29,7 +32,7 @@ describe('createLayoutStore', () => {
     expect(b.store.getSnapshot().sidebar).toBe(SIDEBAR_DEFAULT)
   })
 
-  it('setSidebar/setDetails clamp into the contract ranges', () => {
+  it('setSidebar/setDetails clamp into fixed ranges; setPanel has only a floor', () => {
     const { store, actions } = createLayoutStore().create()
     actions.setSidebar(1)
     expect(store.getSnapshot().sidebar).toBe(SIDEBAR_MIN)
@@ -39,6 +42,15 @@ describe('createLayoutStore', () => {
     expect(store.getSnapshot().details).toBe(DETAILS_MIN)
     actions.setDetails(9999)
     expect(store.getSnapshot().details).toBe(DETAILS_MAX)
+    actions.setPanel(1)
+    expect(store.getSnapshot().panel).toBe(PANEL_MIN)
+    actions.setPanel(9999)
+    expect(store.getSnapshot().panel).toBe(9999)
+    actions.setPanel(460)
+    expect(store.getSnapshot().panel).toBe(460)
+    actions.setPanel(Number.NaN)
+    actions.setPanel(Number.POSITIVE_INFINITY)
+    expect(store.getSnapshot().panel).toBe(460)
   })
 
   it('toggleSidebar flips closed <-> contract default (drag width forgotten)', () => {
@@ -55,7 +67,9 @@ describe('createLayoutStore', () => {
     actions.setSidebar(400)
     actions.setNarrow(true)
     actions.toggleSidebar()
-    expect(store.getSnapshot()).toEqual({ sidebar: 400, details: 0, narrow: true, narrowExpanded: true })
+    expect(store.getSnapshot()).toEqual({
+      sidebar: 400, details: 0, panel: 0, panelExpanded: false, narrow: true, narrowExpanded: true,
+    })
     actions.toggleSidebar()
     expect(store.getSnapshot().narrowExpanded).toBe(false)
     expect(store.getSnapshot().sidebar).toBe(400)
@@ -85,17 +99,50 @@ describe('createLayoutStore', () => {
     expect(store.getSnapshot().details).toBe(0)
   })
 
+  it('togglePanel flips closed <-> contract default; openPanel preserves an open width', () => {
+    const { store, actions } = createLayoutStore().create()
+    actions.togglePanel()
+    expect(store.getSnapshot().panel).toBe(PANEL_DEFAULT)
+    actions.togglePanel()
+    expect(store.getSnapshot().panel).toBe(0)
+    actions.setPanel(460)
+    actions.openPanel()
+    expect(store.getSnapshot().panel).toBe(460)
+    actions.closePanel()
+    expect(store.getSnapshot().panel).toBe(0)
+    actions.openPanel()
+    expect(store.getSnapshot().panel).toBe(PANEL_DEFAULT)
+  })
+
+  it('togglePanelExpanded flips only over an open panel; closing the panel resets the flag', () => {
+    const { store, actions } = createLayoutStore().create()
+    actions.togglePanelExpanded()
+    expect(store.getSnapshot().panelExpanded).toBe(false)
+    actions.togglePanel()
+    expect(store.getSnapshot().panelExpanded).toBe(false)
+    actions.togglePanelExpanded()
+    expect(store.getSnapshot().panelExpanded).toBe(true)
+    actions.closePanel()
+    expect(store.getSnapshot().panelExpanded).toBe(false)
+    actions.togglePanel()
+    expect(store.getSnapshot().panelExpanded).toBe(false)
+  })
+
   it('does not persist panel geometry', () => {
     const first = createLayoutStore().create()
     first.actions.setSidebar(400)
     first.actions.openDetails()
     first.actions.setDetails(500)
+    first.actions.openPanel()
+    first.actions.setPanel(460)
     expect(localStorage.getItem(PERSIST_KEY)).toBeNull()
 
     const second = createLayoutStore().create()
     expect(second.store.getSnapshot()).toEqual({
       sidebar: SIDEBAR_DEFAULT,
       details: 0,
+      panel: 0,
+      panelExpanded: false,
       narrow: false,
       narrowExpanded: false,
     })
diff --git a/packages/client/ui-layout/tests/service.client.spec.ts b/packages/client/ui-layout/tests/service.client.spec.ts
index e1267b4f49..1129d657f9 100644
--- a/packages/client/ui-layout/tests/service.client.spec.ts
+++ b/packages/client/ui-layout/tests/service.client.spec.ts
@@ -1,7 +1,7 @@
 /**
  * LayoutController behavior: the cross-plugin panel-action face. Geometry
  * lives in the entry store (layout-store.spec.ts) — here we assert the
- * delegation contract: attachPanels wiring, the three actions forwarding, the
+ * delegation contract: attachPanels wiring, the action forwarding, the
  * unwired fail-loud, and re-attach overwriting a stale action set.
  */
 import { describe, expect, it, vi } from 'vitest'
@@ -12,15 +12,20 @@ function fakePanels(): PanelActions {
   return {
     setSidebar: vi.fn(),
     setDetails: vi.fn(),
+    setPanel: vi.fn(),
     toggleSidebar: vi.fn(),
     setNarrow: vi.fn(),
     openDetails: vi.fn(),
     closeDetails: vi.fn(),
+    togglePanel: vi.fn(),
+    openPanel: vi.fn(),
+    closePanel: vi.fn(),
+    togglePanelExpanded: vi.fn(),
   }
 }
 
 describe('LayoutController', () => {
-  it('forwards the three panel actions to the attached set', () => {
+  it('forwards the panel actions to the attached set', () => {
     const service = new LayoutController()
     const panels = fakePanels()
     service.attachPanels(panels)
@@ -28,12 +33,21 @@ describe('LayoutController', () => {
     service.toggleSidebar()
     service.openDetails()
     service.closeDetails()
+    service.togglePanel()
+    service.openPanel()
+    service.closePanel()
+    service.togglePanelExpanded()
 
     expect(panels.toggleSidebar).toHaveBeenCalledTimes(1)
     expect(panels.openDetails).toHaveBeenCalledTimes(1)
     expect(panels.closeDetails).toHaveBeenCalledTimes(1)
+    expect(panels.togglePanel).toHaveBeenCalledTimes(1)
+    expect(panels.openPanel).toHaveBeenCalledTimes(1)
+    expect(panels.closePanel).toHaveBeenCalledTimes(1)
+    expect(panels.togglePanelExpanded).toHaveBeenCalledTimes(1)
     expect(panels.setSidebar).not.toHaveBeenCalled()
     expect(panels.setDetails).not.toHaveBeenCalled()
+    expect(panels.setPanel).not.toHaveBeenCalled()
   })
 
   it('fails loud before the root entry wired its actions', () => {
@@ -41,6 +55,10 @@ describe('LayoutController', () => {
     expect(() => { service.toggleSidebar() }).toThrow(/panel actions not wired/)
     expect(() => { service.openDetails() }).toThrow(/panel actions not wired/)
     expect(() => { service.closeDetails() }).toThrow(/panel actions not wired/)
+    expect(() => { service.togglePanel() }).toThrow(/panel actions not wired/)
+    expect(() => { service.openPanel() }).toThrow(/panel actions not wired/)
+    expect(() => { service.closePanel() }).toThrow(/panel actions not wired/)
+    expect(() => { service.togglePanelExpanded() }).toThrow(/panel actions not wired/)
   })
 
   it('re-attach overwrites the stale action set (entry re-register)', () => {
@@ -51,8 +69,11 @@ describe('LayoutController', () => {
     service.attachPanels(fresh)
 
     service.toggleSidebar()
+    service.togglePanel()
 
     expect(stale.toggleSidebar).not.toHaveBeenCalled()
     expect(fresh.toggleSidebar).toHaveBeenCalledTimes(1)
+    expect(stale.togglePanel).not.toHaveBeenCalled()
+    expect(fresh.togglePanel).toHaveBeenCalledTimes(1)
   })
 })
